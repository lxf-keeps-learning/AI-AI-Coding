{
  "name": "ai-ai-coding",
  "version": "0.1.0",
  "private": true,
  "description": "AI 辅助前端编码规范、资产与本地 Prompt Lab",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check js/prompt-utils.js && node --check js/main.js && npm test"
  },
  "engines": {
    "node": ">=18"
  }
}
