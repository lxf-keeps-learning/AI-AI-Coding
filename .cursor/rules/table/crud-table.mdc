---
description: CRUD 列表页：标准结构、useTable + useDialog + BaseSearch
alwaysApply: false
---

## CRUD 列表页规范

### 必须参考

`src/ai-kit/components/list-page-template.vue` —— 所有列表页的标准模板

### 必须使用的 Hooks & 组件

- `useTable` → 表格数据、分页、loading、selection
- `useSearch` → 搜索参数管理、防抖、reset
- `useDialog` → 新增/编辑弹窗状态
- `BaseSearch` → 搜索区域
- `BaseDialog` → 弹窗
- `BaseForm` → 表单（嵌套在 BaseDialog 内）

### 标准页面结构

```
页面
├── BaseSearch（搜索区）
├── 操作栏（新增、批量删除）
├── el-table（v-loading, row-key, @selection-change）
│   ├── el-table-column type="selection"
│   ├── 业务字段列
│   └── 操作列（编辑 / 删除）
├── el-pagination（total, page-sizes）
└── BaseDialog
    └── XxxForm（ref="formRef"）
```

### 删除规范

```ts
async function handleDelete(row: XxxRecord) {
  await ElMessageBox.confirm(`确认删除「${row.name}」？`, '提示', { type: 'warning' })
  await deleteXxx(row.id)
  ElMessage.success('删除成功')
  fetchList()
}
```

### 批量操作

- 批量删除/启停：未勾选时按钮禁用，勾选后展示已选数量。
- 批量操作 loading 期间禁止再次提交。
- 成功后刷新列表并清空选择。
- 批量操作走专用批量接口；无批量接口时按循环单条并提示部分失败。

### 行内编辑

- 可编辑行使用行内 input/select，编辑状态独立管理（编辑行 id + 数据副本）。
- 校验：数量/必填等规则明确，保存失败回滚并提示，取消编辑恢复原值。
- 行内保存调接口，失败回滚；单元格校验提示清晰。
- 批量保存需展示失败行与原因；乐观更新需回滚策略（见 `editable-table`）。

### 性能要求

- tableData 用 shallowRef 或 ref（避免深层响应）
- 避免在 template 中写复杂计算（用 computed）
- 操作列按钮用 link 类型，节省空间
- 分页变化调用 handlePageChange / handleSizeChange，禁止手动 watch pagination
