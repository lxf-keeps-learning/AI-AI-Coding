---
description: 懒加载树：异步子节点、勾选回填、搜索过滤与错误重试
alwaysApply: false
---

## Lazy Tree

- 展开时请求子节点；失败可重试且不打断其他分支。
- 子节点缓存与刷新策略（增删改后局部失效）明确。
- 占位 skeleton 与空子节点文案区分。

## 勾选与回填

- 勾选场景必须明确回填与提交语义：编辑时先按角色/权限回填已勾选 id。
- 半选节点语义明确（只回传勾选 id 或勾选 + 半选 id，与后端约定一致）。
- 子树懒加载不影响已勾选状态的回填与展示。

## 搜索过滤

- 关键词过滤：命中节点及其祖先高亮/展开，无结果显示空状态。
- 大数据量懒加载树搜索建议走服务端搜索接口，避免全量内存过滤。
- 搜索时 loading，搜索结果与懒加载状态互不干扰。
