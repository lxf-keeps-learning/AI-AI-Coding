---
description: useTable：列表分页、错误、并发结果和选择状态管理
alwaysApply: false
---

## useTable

位置：`src/ai-kit/hooks/useTable.ts`

```ts
interface UserQuery extends Record<string, unknown> {
  name: string
  status?: number
}

const table = useTable<UserRecord, UserQuery>(getUserList)
onMounted(table.fetchList)
```

- API 在 service 层适配为 `{ list: T[]; total: number }`。
- 搜索调用 `handleSearch`，重置调用 `handleReset`，不要直接改内部查询参数。
- 分页事件直接绑定 `handlePageChange` / `handleSizeChange`。
- 页面必须渲染 `error` 和重试入口。
- hook 只允许最后一次请求更新列表；真正中止网络请求仍在 service 层处理。
