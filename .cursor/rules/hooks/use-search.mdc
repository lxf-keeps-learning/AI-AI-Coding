---
description: useSearch：搜索参数、防抖触发和重置管理
alwaysApply: false
---

## useSearch

位置：`src/ai-kit/hooks/useSearch.ts`

```ts
const search = useSearch<UserQuery>(
  { name: '', status: undefined },
  table.handleSearch
)
```

- 搜索按钮调用 `searchImmediately()`，实时输入联想调用 `search()`。
- `reset()` 恢复初始值并立即触发查询。
- 回调接收普通对象快照，不要依赖内部 reactive 引用。
- URL query 同步属于业务/路由层职责，当前 hook 不提供自动路由同步。
- 与 BaseSearch、useTable 配套使用。
