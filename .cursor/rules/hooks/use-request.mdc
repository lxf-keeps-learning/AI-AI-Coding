---
description: useRequest：异步请求状态、并发结果与刷新管理
alwaysApply: false
---

## useRequest

位置：`src/ai-kit/hooks/useRequest.ts`

```ts
const request = useRequest(getUserById)
await request.run(userId)

const immediateRequest = useRequest(getUserById, {
  immediate: true,
  defaultParams: [userId],
  initialData: null,
})
```

规则：

- 页面级 `data/loading/error` 优先使用 useRequest，分页列表使用 useTable。
- 有参数的 `immediate` 请求必须提供 `defaultParams`。
- `cancel()` 仅使当前响应失效；需要真正中止 HTTP 时，在 service 层使用 AbortController。
- 组件必须渲染 `error` 和重试入口，不能只显示 loading。
- API 放在 services，hook 内不写业务判断。
