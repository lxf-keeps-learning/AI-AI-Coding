# HLD-<slug>-概要设计

> 状态：评审中 ｜ 版本：v1.0 ｜ 需求 slug：<slug>
> 负责人：@研发    上游：design/<slug>/PRD-SPEC-<slug>.md
> 变更记录：v1.0 <日期> 初始创建

## 1. 设计目标
- 一句话：本需求要交付什么前端能力

## 2. 架构与模块划分
- 页面/路由结构（新增哪些路由，继承哪些布局）
- 模块清单：页面模块 / 组件模块 / Hook 模块 / 状态模块

## 3. 接口契约（★ 跨端唯一真相，供 LLD/TASKS 引用）
| 接口 | 方法 | 入参 | 出参 | 错误码 | 使用页面 |
|------|------|------|------|--------|---------|
| /api/report/summary | GET | {date} | {kpi[], trend[]} | 4xx/5xx | 报表页 |

## 4. 状态管理设计
- 全局 Store：哪些状态进 Pinia（按模块命名）
- 本地状态：组件内 ref/computed 即可

## 5. 关键交互与数据流
- 时序：页面加载 → 请求 → 渲染 → 交互 → 更新
- 异常分支：加载中/空数据/接口失败/权限不足

## 6. 公共资产复用（★ 必须复用 ai-kit）
| 能力 | 复用 ai-kit 哪个 | 位置 |
|------|----------------|------|
| 列表展示 | ai-kit/table | src/ai-kit/table |
| 搜索表单 | ai-kit/search | src/ai-kit/search |
| 图表 | ai-kit/charts | src/ai-kit/charts |

## 7. 关键技术决策（ADR）
- 决策：为什么这么选（如"图表用 ECharts 而非自研"）
- 备选方案与放弃原因

## 8. 风险与依赖
- 依赖：后端接口未就绪？第三方库版本？
- 风险：数据量大/兼容性/权限
