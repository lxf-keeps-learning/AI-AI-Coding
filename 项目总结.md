# AICoding 项目总结

> 更新时间：2026-08-20
> 当前版本：0.1.0（`private: true`，未发布到 npm）
> 项目类型：开发期 AI 工程基础设施（面向 Vue 3 + TypeScript 团队）

## 1. 一句话定位

AICoding 是面向 Vue 3 + TypeScript 团队的**开发期 AI 工程基础设施**：让 Codex、Cursor 等编码助手基于团队规范、可复用资产和真实业务项目生成代码。它不是业务运行时系统，也不是成熟的 UI 组件库。

核心原则：

> 不让 AI 从 0 随机生成，让 AI 基于规范、Prompt、公共资产和业务事实生成。

## 2. 项目背景和问题

团队直接使用通用 AI 编码工具时，常见问题包括：

- AI 不知道项目已有的组件和 Hook，重复实现弹窗、请求、分页、防抖和图表生命周期；
- 同一种页面由不同开发者生成后，目录、命名、错误处理和交互行为不一致；
- Prompt 留在个人聊天记录里，难以复用、评审和版本化；
- AI 容易虚构 API、字段、权限或依赖，代码看似完整但无法接入真实项目；
- 规范只存在于文档，往往等到 Code Review 才发现问题，返工较晚；
- 公共组件的文档和真实代码可能漂移，AI 按错误契约继续放大问题。

AICoding 将这些问题前移到生成阶段处理，并用自动检查守住规则和实现的一致性。

## 3. 整体架构

```
AICoding（开发期 AI 工程基础设施）
├── 约束层   .cursor/rules/          ← AI 生成约束（50 条 .mdc）
├── 输入层   prompts/                ← 可版本化任务模板（22 个非索引 Prompt）
├── 事实层   src/ai-kit/             ← Vue3 + TS 参考实现（30 个 .ts/.vue）
├── 流程层   skills/                 ← 可执行工作流（6 个 Skill）
├── 需求层   design/                 ← PRD-SPEC/HLD/LLD/TASKS 模板与样例
├── 工具层   scripts/                ← CLI、检索器、安装器、目录生成、契约检查
├── 交互层   index.html + js/ + css/ ← 本地 Prompt Lab
└── 验证层   tests/ + npm scripts    ← Node 测试、Vitest、vue-tsc、契约检查
```

四个核心资产分别回答不同问题：

| 资产 | 回答的问题 | 示例 |
|------|-----------|------|
| Cursor Rules | 不能怎么做 | 禁止 `any`、禁止裸用 `el-dialog` |
| Prompts | 一次任务要什么 | 折线图 Prompt：字段、交互、验收 |
| ai-kit | 具体怎么写 | `useRequest`、`BaseChart` 的真实实现 |
| Skills | 完整流程怎么走 | prd-spec-enhancer、aicoding-codegen、code-reviewer |

## 4. 两条核心工作流

### 工作流一：需求链路（仓库内，模板 + Skill 驱动）

```text
MRD/需求 → PRD-SPEC → HLD → LLD → TASKS
```

- PRD-SPEC 由 `skills/prd-spec-enhancer/` 生成（仓库内 Skill）；
- HLD / LLD / TASKS 由 `design/_templates/` 模板驱动产出，不是独立自动化 Skill；
- 真实样例：`design/cascade-filter/`（三级级联筛选）。

### 工作流二：编码链路（可安装到业务项目）

```text
需求 → Prompt 检索 → Cursor Rules/ai-kit → 业务项目代码生成 → 验证
```

```text
开发者在真实业务项目提出需求
        │
        ▼
.aicoding/config.json 定位 AICoding 知识库
        │
        ▼
search-prompts 根据自然语言检索 Prompt
        ├── 匹配全局与场景 Cursor Rules
        ├── 读取 Prompt 中的 ai-kit references
        └── 返回命中分数、预览和来源路径
        │
        ▼
Codex Skill / Cursor Rule 检查业务项目事实
        ├── package.json 与已有依赖
        ├── 相似页面、组件、Hook
        ├── API、类型、权限与目录约定
        └── 测试、类型检查和构建命令
        │
        ▼
在业务项目内生成或修改代码 → 运行项目已有验证 → 报告依据
```

## 5. 核心模块

### 5.1 Spec（PRD-SPEC）

- **实现方式**：`skills/prd-spec-enhancer/`（仓库内 Skill）+ `design/_templates/PRD-SPEC模板.md`。
- **核心机制**：checklist 逐项检查 → 必要性判定（必备/条件必备/可选）→ 自动补全 → 自检 → 一次性交互收集缺口 → 按模板渲染。
- **来源标注**：默认不标即已确认；`≈推断`（AI 补全需复核）、`⚠️待确认`（未定需补齐）。
- **边界**：当前是仓库内 Skill，安装器尚未实现 `--with-spec`，不能一键安装到业务项目。

### 5.2 Prompt（可版本化任务模板）

- 当前 22 个非索引 Prompt，覆盖 charts / components / forms / git / hooks / pages / performance / refactor / review / search / table / tree。
- 检索器（`scripts/search-prompts.js`）按自然语言关键词、别名、标题、路径和正文评分，返回命中 Prompt 及其关联的 `rules` 和 `references`。
- 目录由 `scripts/build-prompt-catalog.js` 生成（`npm run build:prompts`），`check:prompts` 保证静态目录与源文件一致。
- 附带 `prompts/viewer.*`（Prompt 可视化浏览工具）与本地 Prompt Lab（`index.html`）。

### 5.3 Cursor Rules（生成约束）

- 当前 50 条 `.mdc`，覆盖 global / components / forms / search / table / tree / charts / hooks / pages / performance / review / refactor。
- 基础搜索、表单、弹窗、树和图表具有真实 ai-kit 实现；高级搜索、上传、虚拟表格、地图和大屏等属于生成指导，生成前必须检查业务项目依赖。

### 5.4 ai-kit（参考实现）

当前 30 个 Vue/TypeScript 实现文件，包括：

| 类型 | 资产 | 已验证能力 |
| --- | --- | --- |
| 容器组件 | `BaseDialog`、`BaseDrawer` | loading 锁定、异步关闭拦截、错误上报、响应式尺寸 |
| 表单与搜索 | `BaseForm`、`BaseSearch` | 统一校验、1/2 列布局、超过 3 项折叠、移动端单列 |
| 数据展示 | `BaseChart`、`BaseTree` | loading、empty、error、retry 与生命周期管理 |
| 级联筛选 | `CascadeFilter` + `useCascadeOptions` | 三级联动、懒加载选项链 |
| 状态 Hook | `useRequest`、`useTable`、`useTree` | 错误状态、重试、最后请求优先 |
| 交互 Hook | `useDialog`、`useSearch`、`useChart` | Promise 收敛、防抖清理、图表实例与销毁 |
| 工具 | `src/ai-kit/utils/`（13 个） | 防抖、节流、日期、格式、校验、存储、DOM、文件、树、数组、对象、字符串 |
| 模板 | `list-page-template.vue` | 可参与类型检查的列表页参考结构 |

ai-kit 默认是生成参考。业务项目已有同类公共组件时优先复用业务项目；只有业务项目已复制或配置对应 ai-kit，才能直接 import。

### 5.5 Skills（可执行工作流，6 个）

| Skill | 类型 | 说明 |
|-------|------|------|
| `aicoding-codegen` | ✅ 可安装到业务项目 | 检索 Prompt → 读取 Rules/ai-kit → 业务项目生成并验证 |
| `prd-spec-enhancer` | 仓库内 | PRD/需求 → PRD-SPEC |
| `code-reviewer` | 仓库内 | git diff → 规则库扫描 → Markdown 报告（PASS/FAIL/CONDITIONAL） |
| `engineering` | 仓库内 | 识别重复 → 判定抽取 → 沉淀到 ai-kit |
| `performance` | 仓库内 | 症状采集 → 瓶颈分类 → 优化报告 |
| `grilling` | 仓库内 | 方案/叙事 → 决策树逐层追问 |

### 5.6 CLI / 安装器

- CLI：`scripts/aicoding.js`，提供 `install` 与 `search` 两个子命令。
- 安装器：`scripts/install-integration.js`，默认只生成三样东西：
  ```text
  .aicoding/config.json
  .agents/skills/aicoding-codegen/
  .cursor/rules/aicoding-codegen.mdc
  ```
- 检索器：`scripts/search-prompts.js`，自然语言 → Prompt + Rules + references。

### 5.7 Prompt Lab

浏览器直接打开 `index.html` 即可使用，不依赖后端。支持：Prompt 目录展示、分类筛选、全文搜索、一键插入、`{{变量}}` 替换、格式化、复制、草稿自动保存（400ms 防抖）、最多 50 条本地历史快照。数据存于浏览器 localStorage。

### 5.8 验证体系

```bash
npm run check
```

包含：语法检查（node --check）、Prompt 目录一致性（check:prompts）、ai-kit 契约（check:ai-kit-contracts）、TypeScript（vue-tsc）、Node 测试（test:node）、Vitest（test:ai-kit）。

## 6. 当前可验证结果

以最终命令输出为准，当前 `npm run check` 全绿：

- 50 条 Cursor 规则契约检查通过；
- 30 个 ai-kit 实现文件通过严格类型检查（vue-tsc 0 错误）；
- Node 测试 10 项通过；
- Vitest 3 个测试文件、10 项测试通过；
- Prompt 静态目录与源文件一致；
- `npm pack --dry-run` 成功，发布清单约 130 个文件，由 `package.json#files` 白名单控制，包含 Rules、Prompt、ai-kit、CLI 与 codegen Skill。

这些结果证明的是“仓库自身契约可执行”，不等同于已经证明所有业务项目都能零修改接入，也不等同于真实业务试点取得了效率收益。

## 7. 项目完整度评价

| 维度 | 状态 |
|------|------|
| 资产边界 | 已明确：正式资产、过程文档、独立实验和缓存由发布白名单区分；正式新增资产仍需随本次变更提交 |
| 验证链 | ✅ `npm run check` 全绿 |
| 发布边界 | ✅ `npm pack --dry-run` 通过，白名单不含 tests/实验/缓存 |
| 安装器 | ✅ 默认安装 Codegen（config.json + Skill + Rule） |
| Spec 安装 | ⚠️ 未实现 `--with-spec`，PRD-SPEC 仍是仓库内 Skill |
| 真实业务试点 | ⚠️ 未进行，效率/质量收益未验证 |
| CI | ⚠️ 未配置 |

## 8. 技术亮点

- **规则驱动 + 机械判定**：code-reviewer 把 `.cursor/rules/` 与 `src/ai-kit/` 提炼为可机械判定的审查规则，凡重复封装 ai-kit 已有能力一律记为 Error。
- **渐进式上下文**：检索器只返回最相关 1–3 个 Prompt 及对应 Rules/references，不一次加载整个仓库。
- **防漂移**：`validate-ai-kit.js` 检查 `.mdc` frontmatter、引用的 ai-kit 路径存在性、源码无 `any`，配合 vue-tsc 与测试。
- **可运行性契约**：`useRequest` 后发请求优先、`useDialog` Promise 收敛、`useChart` 挂载前状态缓存与 dispose 清理。
- **发布边界唯一白名单**：`package.json#files` 精确控制 npm 包内容，实验/测试/过程文档不进入发布。

## 9. 实际使用方式

```bash
# 仓库内验证
npm ci
npm run build:prompts     # 修改 prompts/**/*.md 后
npm run check

# 接入业务项目
cd /你的/业务项目
npm install --save-dev /AICoding所在路径/AI-AI-Coding
npx aicoding install --target .

# 调试检索
npx aicoding search "折线图"
```

## 10. 边界和不足

1. 项目仍为 `private: true`，尚未发布到公共或内部 npm 仓库；
2. 已有完整本地 `npm run check`，但仓库中尚未配置 CI；
3. ai-kit 是参考实现集合，不是具备独立文档站、主题系统和版本兼容承诺的成熟组件库；
4. Prompt 检索基于关键词和规则评分，不是向量检索，对同义表达的覆盖取决于别名与内容质量；
5. PRD-SPEC / code-reviewer / engineering / performance / grilling 是仓库内 Skill，未随安装器分发；
6. HLD / LLD / TASKS 是模板驱动，不是独立自动化 Skill；
7. Prompt Lab 使用 localStorage，不支持跨设备同步、多人协作和敏感信息治理；
8. 没有真实业务项目的长期度量数据，价值模型已定义但实际收益仍需试点验证；
9. 生成结果仍必须经过目标项目的测试、Review 和人工业务判断。

## 11. 后续路线

### P0：真实业务试点

- 选择 CRUD、图表、Dialog 三类高频且边界清晰的任务；
- 记录接入前基线与接入后数据（开发周期、人工修改时长、Review 问题、缺陷逃逸）；
- 收集未命中 Prompt、错误引用和二次修改原因，用数据决定规则去留。

### P1：补齐团队交付能力

- 接入 CI，运行 `npm run check` 与安全审计；
- 发布到内部 npm 或固定 Git tag，建立版本和变更日志；
- 评估 `--with-spec` 安装扩展（把 prd-spec-enhancer 与设计模板安装到业务项目）——当前未实现；
- 为 Prompt 检索增加可解释的失败反馈和质量评估集。

### P2：按证据扩展

- 至少两个稳定业务用例后再抽取上传、预览、虚拟表格等资产；
- 评估规则检索与向量检索的混合方案，但不为技术复杂度而升级。

## 12. 总结

AICoding 已从“提示词和规范仓库”演进为一条可安装、可检索、可生成、可检查的开发期工作流：需求链路（PRD-SPEC → HLD → LLD → TASKS）与编码链路（检索 → 规则/资产 → 生成 → 验证）各有支撑资产与验证体系。它当前最重要的下一步不是继续堆功能，而是在真实业务项目中用交付时间、复用率、Review 问题和缺陷数据证明价值。
