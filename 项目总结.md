# AI-AI-Coding 项目总结

> 更新时间：2026-07-14  
> 当前版本：0.1.0  
> 项目类型：AI 辅助研发知识库、代码生成工作流与 Vue 3 公共资产参考实现

## 1. 项目定位

AI-AI-Coding 面向 Vue 3 前端团队，目标不是单纯提高 AI 的代码输出量，而是让 Codex、Cursor 等编码助手基于团队规范、已有组件和真实业务项目生成代码。

项目核心原则是：

> 不让 AI 从 0 随机生成，让 AI 基于规范、Prompt、公共资产和业务事实生成。

它不是一个需要部署给终端用户的业务系统，也不应成为业务代码的运行时绝对路径依赖。它更接近一套“开发期 AI 工程基础设施”：开发者在真实业务仓库提出需求，AICoding 负责检索合适的 Prompt、Cursor 规则和 ai-kit 参考实现，AI 再结合业务仓库中的依赖、接口、类型和现有代码完成实现与验证。

## 2. 要解决的真实问题

团队直接使用通用 AI 编码工具时，常见问题包括：

- AI 不知道项目已有的组件和 Hook，重复实现弹窗、请求、分页、防抖和图表生命周期；
- 同一种页面由不同开发者生成后，目录、命名、错误处理和交互行为不一致；
- Prompt 留在个人聊天记录里，难以复用、评审和版本化；
- AI 容易虚构 API、字段、权限或依赖，代码看似完整但无法接入真实项目；
- 规范只存在于文档，往往等到 Code Review 才发现问题，返工较晚；
- 公共组件的文档和真实代码可能漂移，AI 按错误契约继续放大问题。

AICoding 将这些问题前移到生成阶段处理，并用自动检查守住规则和实现的一致性。

## 3. 最新端到端工作流

```text
开发者在真实业务项目提出需求
        │
        ▼
.aicoding/config.json 定位 AICoding 知识库
        │
        ▼
search-prompts 根据自然语言检索 Prompt
        │
        ├── 匹配全局与场景 Cursor Rules
        ├── 读取 Prompt 中的 ai-kit references
        └── 返回命中分数、预览和来源路径
        │
        ▼
Codex Skill / Cursor Rule 检查业务项目事实
        │
        ├── package.json 与已有依赖
        ├── 相似页面、组件、Hook
        ├── API、类型、权限与目录约定
        └── 测试、类型检查和构建命令
        │
        ▼
在业务项目内生成或修改代码
        │
        ▼
运行业务项目已有验证并报告依据
```

例如输入“生成一个折线图”，当前检索器会命中 `prompts/charts/line-chart.md`，并自动关联全局基础规则、TypeScript 规则、图表基础规则和折线图专项规则。AI 不会只复制模板，而是继续检查目标项目是否已有图表封装、ECharts 版本和数据接口。

## 4. 核心模块与当前能力

### 4.1 `.cursor/rules/`：生成约束

当前共有 50 条 `.mdc` 规则，覆盖：

- 全局架构、命名、TypeScript 和 Git；
- Dialog、Drawer、表单、搜索、表格、树和图表；
- 页面模式、性能、Review 与重构。

规则不再把所有示例都描述成已实现组件。基础搜索、表单、弹窗、树和图表具有真实 ai-kit 实现；高级搜索、上传、虚拟表格、地图和大屏等目前属于生成指导，生成前必须检查业务项目依赖。

### 4.2 `prompts/`：可版本化的任务模板

当前包含 17 个非索引 Prompt，覆盖图表、CRUD、表单、组件、Hook、树、性能、重构和 Review 等场景。

检索器不是简单按文件名查找，而是综合自然语言关键词、别名、标题、路径和正文评分。结果还会返回对应 `rules` 和 `references`，使 Prompt、规则与代码资产进入同一条上下文链路。

### 4.3 `src/ai-kit/`：可信的参考实现

当前有 15 个 Vue/TypeScript 实现文件，主要包括：

| 类型 | 资产 | 已验证能力 |
| --- | --- | --- |
| 容器组件 | `BaseDialog`、`BaseDrawer` | loading 锁定、异步关闭拦截、错误上报、响应式尺寸 |
| 表单与搜索 | `BaseForm`、`BaseSearch` | 统一校验、1/2 列布局、超过 3 项折叠、移动端单列 |
| 数据展示 | `BaseChart`、`BaseTree` | loading、empty、error、retry 与生命周期管理 |
| 状态 Hook | `useRequest`、`useTable`、`useTree` | 错误状态、重试、最后请求优先，防止旧响应覆盖 |
| 交互 Hook | `useDialog`、`useSearch`、`useChart` | Promise 收敛、防抖清理、实时图表实例与销毁 |
| 工具 | `debounce`、`throttle` | `cancel`、尾调用控制，debounce 支持 `flush` |
| 模板 | `list-page-template.vue` | 可参与类型检查的列表页参考结构 |

ai-kit 默认是生成参考。如果业务项目已经有同类公共组件，优先复用业务项目；只有在业务项目已复制或配置对应资产时，才能直接 import。

### 4.4 `skills/`：可执行工作流

当前有三个 Skill：

- `aicoding-codegen`：在真实业务仓库完成 Prompt 检索、规则读取、代码生成和验证；
- `engineering`：审核重复实现，判断应该复用、抽取还是继续观察；
- `performance`：按证据诊断渲染、内存、网络、计算、图表和大数据问题。

与普通 Prompt 相比，Skill 具有明确的触发条件、执行步骤、边界和交付要求。

### 4.5 Prompt Lab：本地 Prompt 工作台

浏览器直接打开 `index.html` 即可使用，不依赖后端。当前支持：

- 展示 17 个项目 Prompt；
- 分类筛选、全文搜索、来源路径展示和一键插入；
- `{{变量}}` 扫描与替换、格式化和复制；
- 400ms 防抖草稿保存与启动恢复；
- 最多 50 条本地历史快照；
- 保存、复制快捷键和本地数据结构校验。

Prompt 目录由 `prompts/**/*.md` 自动生成，修改 Prompt 后执行 `npm run build:prompts`，避免首页数据靠人工维护。

### 4.6 业务项目安装器

推荐以开发依赖接入：

```bash
npm install --save-dev /path/to/AI-AI-Coding
npx aicoding install --target .
```

安装器会在业务项目生成：

```text
.aicoding/config.json
.agents/skills/aicoding-codegen/
.cursor/rules/aicoding-codegen.mdc
```

这样 Codex 和 Cursor 可以在业务仓库中感知 AICoding，而生成的代码仍然落在业务仓库并遵守业务项目约定。

## 5. 最新工程化改造

本轮改造重点不是增加更多概念组件，而是让已有资产真的可用：

1. 修复 `useChart` 返回失效实例、挂载前 option/loading 丢失、主题重建和清理问题；
2. 为 `useRequest`、`useTable`、`useTree` 增加错误状态、重试与并发保护；
3. 修复 `useDialog` 重复打开导致上一次 Promise 悬空的问题；
4. 完善 Dialog、Drawer、Search、Form、Chart、Tree 的真实交互契约；
5. 纠正规则中“文档声称支持、代码没有实现”的内容；
6. 让 Prompt 检索结果自动关联全局规则、领域规则和同名场景规则；
7. 新增 Vue SFC 类型检查、Hook/组件测试和 ai-kit 契约检查；
8. 将 ECharts 升级至 6.1.0，清除安全审计发现的 XSS 风险；
9. 验证 npm 发布清单确实包含 Cursor Rules、Prompt、ai-kit、CLI 和 codegen Skill。

## 6. 当前可验证结果

最新执行 `npm run check` 的结果：

- 50 条 Cursor 规则契约检查通过；
- 15 个 ai-kit Vue/TypeScript 文件通过严格类型检查；
- Node 测试 9 项通过；
- Vitest 3 个测试文件、10 项测试通过；
- Prompt 静态目录与源文件一致；
- ECharts 为 6.1.0，最近一次 npm 安全审计为 0 个漏洞；
- npm 打包预检包含 107 个文件，Rules、Prompt、ai-kit、CLI 和 Skill 均进入发布清单。

这些结果证明的是“仓库自身契约可执行”，不等同于已经证明所有业务项目都能零修改接入。

## 7. 对研发效率和业务的价值

### 7.1 研发效率价值

- **减少需求澄清后的起步时间**：开发者不必从聊天记录寻找 Prompt，也不必反复告诉 AI 项目通用规范；
- **减少重复实现**：常见 loading、error、pagination、dialog、chart lifecycle 可以复用已有契约；
- **减少低价值 Review**：类型、`any`、规则引用、Prompt 目录漂移等问题由自动检查提前发现；
- **降低新人上手成本**：Prompt、规则和代码实现共同展示团队认可的页面结构；
- **缩短失败反馈周期**：AI 在生成后运行目标项目已有检查，而不是只交付一段代码。

### 7.2 业务价值

AICoding 不直接创造业务收入，它通过研发链路间接影响业务：

- 缩短常规页面和图表需求的交付周期，使需求更早上线；
- 降低同类交互不一致带来的用户体验和维护问题；
- 减少并发覆盖、提交中误关闭、资源未销毁等常见缺陷进入生产环境；
- 将团队经验从个人能力转化为可复用资产，降低关键人员依赖；
- 让工程规则可追踪、可测试，降低长期维护成本。

### 7.3 如何量化价值

当前仓库没有真实业务试点数据，因此不能把目标值描述成已经取得的收益。建议在 2—4 周试点中采集：

| 维度 | 指标 | 计算方式 |
| --- | --- | --- |
| 交付效率 | 需求开发周期 | 从首次编码到提交可 Review PR 的工作时长 |
| AI 有效性 | 首次生成可用率 | 无需推翻重写的任务数 / AI 生成任务数 |
| 修改成本 | 人工修改时长 | 生成完成后到通过本地检查的时长 |
| 复用程度 | ai-kit/已有资产复用率 | 使用已有公共资产的适用任务数 / 适用任务总数 |
| 质量 | Review 问题密度 | 规范与重复实现问题数 / PR 或变更千行数 |
| 稳定性 | 生成代码缺陷逃逸率 | 上线后归因于生成代码的缺陷数 / 生成任务数 |
| 业务响应 | 需求前置时间 | 需求进入开发到上线的自然日或工作日 |

评估时应选择相似复杂度任务，记录接入前基线，再采用同团队前后对比或小范围对照组。效率提升不能以测试减少、缺陷增加为代价，因此必须同时观察质量护栏。

## 8. 项目边界与不足

需要如实说明当前边界：

- 已有本地安装和 npm 打包预检，但 `private: true`，尚未正式发布到公共或内部 npm 仓库；
- 已有完整本地 `npm run check`，但仓库中尚未配置 CI；
- ai-kit 是参考实现集合，不是具备独立文档站、主题系统和版本兼容承诺的成熟组件库；
- Prompt 检索目前基于规则和关键词评分，不是向量检索，对同义表达的覆盖取决于别名与内容质量；
- Prompt Lab 使用 `localStorage`，不支持跨设备同步、多人协作和敏感信息治理；
- 目前没有真实业务项目的长期度量数据，价值模型已经定义，但实际收益仍需试点验证；
- 生成结果仍必须经过目标项目的测试、Review 和人工业务判断。

## 9. 下一阶段建议

### P0：完成真实业务试点

- 选择 CRUD、图表、Dialog 三类高频且边界清晰的任务；
- 记录接入前基线与接入后数据；
- 收集未命中 Prompt、错误引用和二次修改原因；
- 用数据决定哪些规则需要加强或删除。

### P1：补齐团队交付能力

- 接入 CI，运行 `npm run check` 和安全审计；
- 发布到内部 npm 或固定 Git tag，建立版本和变更日志；
- 增加最小业务示例仓库，验证安装、生成、构建和升级流程；
- 为 Prompt 检索增加可解释的失败反馈和质量评估集。

### P2：按证据扩展平台能力

- 只有出现至少两个稳定业务用例后，再抽取上传、预览、虚拟表格等资产；
- 根据团队需求评估收藏、导入导出、协作和权限治理；
- 评估规则检索与向量检索的混合方案，但不为技术复杂度而升级。

## 10. 一句话总结

AI-AI-Coding 已从“提示词和规范仓库”演进为一条可安装、可检索、可生成、可检查的开发期工作流；它当前最重要的下一步不是继续堆功能，而是在真实业务项目中用交付时间、复用率、Review 问题和缺陷数据证明价值。
