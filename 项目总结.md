# AICoding 项目总结

> 更新时间：2026-08-20
> 当前版本：0.1.0（`private: true`，尚未发布到 npm）
> 项目类型：面向 Vue 3 + TypeScript 团队的开发期 AI 工程基础设施
> 事实口径：以当前仓库代码、`npm run check` 和 `npm pack --dry-run` 的实际结果为准

## 1. 项目结论

AICoding 已经从“零散 Prompt 和编码规范”演进为一套可安装、可检索、可生成、可检查的开发期 AI 工程基础设施。它把团队规范、任务模板、公共资产和 Agent 工作流连接起来，让 Codex、Cursor 等编码助手基于团队事实生成代码，而不是从零随机生成。

项目目前完成了仓库级工程闭环，并具备一个可跨项目安装的代码生成 Skill；但尚未完成内部 npm 发布、CI、组织级推广和真实业务试点。因此当前能够证明的是：

- 规则、Prompt、ai-kit 和 Skill 已形成可版本化资产；
- Prompt 与 Rules、ai-kit 的关系已有权威合同，不再只依赖目录或文本猜测；
- Codegen 可以安装到业务项目，在业务仓库中检索知识、生成代码并执行验证；
- 本地质量门控全绿，核心工程契约可执行；
- 组织效率与质量收益已有清晰的验证模型，但还不能宣称已经取得具体提升比例。

一句话定位：

> 不让 AI 从 0 随机生成，让 AI 基于组织规范、可复用资产和真实业务事实完成交付。

## 2. 当前完成度总览

| 维度 | 当前状态 | 证据与边界 |
| --- | --- | --- |
| Cursor Rules | 已实现 | 50 条 `.mdc`，覆盖全局、组件、表单、搜索、表格、树、图表、Hook、页面、性能、Review、重构等场景 |
| Prompt 资产 | 已实现 | 22 个正式 Prompt，可检索、可浏览、可版本化 |
| Prompt 资产合同 | 已实现 | 22/22 Prompt 均声明对应 Rules 与 ai-kit References；缺少运行时资产时必须说明原因 |
| ai-kit | 已实现并通过类型检查 | 30 个 `.ts/.vue` 参考实现文件，包括组件、Hook、工具和模板；定位是生成参考，不是成熟运行时组件库 |
| 项目 Skills | 已实现 6 个 | 1 个可安装到业务项目，5 个当前为仓库内工作流 |
| CLI 与安装器 | 已实现基础版 | 支持 `install` 和 `search`；默认只安装 Codegen Skill、Cursor Rule 和配置 |
| Prompt Lab | 已实现 | 本地浏览器使用，支持检索、变量替换、格式化、复制、草稿与历史快照 |
| 本地验证 | 已完成 | `npm run check` 全绿：Node 22/22、Vitest 10/10、类型检查和契约检查通过 |
| 发布边界 | 已定义但未收口 | `npm pack --dry-run` 可生成 131 项清单，但白名单缺少 `scripts/prompt-contracts.js` 运行时依赖，当前不具备发布就绪状态 |
| CI | 未实现 | 仓库中尚无自动化流水线，当前依赖本地执行检查 |
| 真实业务试点 | 未开展 | 尚无开发周期、Review 缺陷、复用率等真实对比数据 |

## 3. 项目解决的问题

团队直接使用通用 AI 编码工具时，常见问题不是“AI 不会写代码”，而是“AI 不知道组织内部事实”：

- 不知道已有组件和 Hook，重复实现弹窗、请求、分页、防抖、树和图表生命周期；
- 同类页面由不同开发者生成后，目录、命名、状态管理、异常处理和交互行为不一致；
- Prompt 停留在个人聊天记录里，无法复用、评审、度量和版本化；
- 容易虚构 API、字段、权限、依赖或组件能力，代码看似完整却无法接入业务项目；
- 规范主要在 Code Review 阶段生效，问题发现晚、返工成本高；
- 规范文档、Prompt 与真实代码可能漂移，AI 会持续放大错误知识；
- 公共经验依赖少数资深开发者口头传递，难以转化为组织资产。

AICoding 的核心做法是把规则和知识前移到需求理解、代码生成和审查阶段，并用机械检查维护资产之间的一致性。

## 4. 整体架构

```text
AICoding（开发期 AI 工程基础设施）
├── 约束层   .cursor/rules/          AI 生成与审查规则
├── 输入层   prompts/                可版本化任务模板
├── 合同层   prompts/asset-contracts.json
│                                      Prompt → Rules → ai-kit 权威映射
├── 事实层   src/ai-kit/             Vue 3 + TypeScript 参考实现
├── 流程层   skills/                 6 个可执行工作流
├── 需求层   design/                 PRD-SPEC/HLD/LLD/TASKS 模板与样例
├── 工具层   scripts/                CLI、检索、安装、目录和契约检查
├── 交互层   index.html + workbench/ 本地 Prompt Lab 与交付工作台
└── 验证层   tests/ + npm scripts    类型、契约和行为测试
```

核心资产分工：

| 资产 | 回答的问题 | 例子 |
| --- | --- | --- |
| Rules | 不能怎么做、必须遵守什么 | 禁止 `any`、禁止重复封装 ai-kit 已有能力 |
| Prompts | 一次具体任务需要什么 | 折线图、CRUD 表格、表单、Dialog 的输入和验收 |
| Asset Contracts | Prompt 应加载哪些事实 | 折线图 Prompt 明确关联图表 Rules、`BaseChart`、`useChart`、`useRequest` |
| ai-kit | 团队推荐的实现方式是什么 | `useRequest`、`BaseDialog`、`BaseChart` 等真实代码 |
| Skills | 一个完整任务应该按什么流程执行 | 需求补全、代码生成、审查、性能诊断、资产沉淀 |

## 5. 两条核心交付链路

### 5.1 需求设计链

```text
MRD / 原始需求 → PRD-SPEC → HLD → LLD → TASKS
```

当前事实：

- `prd-spec-enhancer` 已经是可执行 Skill；
- HLD、LLD、TASKS 当前由 `design/_templates/` 模板驱动；
- `design/cascade-filter/` 提供了三级级联筛选的完整样例；
- 自动生成 HLD、LLD、TASKS 的专业 Skills 与编排器已有设计和实施计划，但仓库中尚无对应 Skill 目录和校验脚本，因此仍属于规划能力。

### 5.2 编码交付链

```text
业务需求
  → 检索相关 Prompt
  → 根据资产合同加载 Rules 与 ai-kit References
  → 检查业务项目技术栈、依赖、API、类型和相似实现
  → 在业务仓库生成或修改代码
  → 执行业务项目已有检查
  → 报告生成依据与未确认项
```

这条链路由 `aicoding-codegen`、检索器、安装器和验证体系共同支撑，是当前项目跨仓库使用最完整的能力。

## 6. 现有 6 个 Skill：能力、使用情况与组织价值

### 6.1 总览

| Skill | 核心技能 | 当前使用形态 | 当前成熟度 | 主要组织价值 |
| --- | --- | --- | --- | --- |
| `aicoding-codegen` | 检索组织知识后在业务项目生成并验证代码 | 可由安装器写入业务项目 | 已实现、可安装、已有安装与检索测试 | 提升规范遵守率、资产复用率和生成结果可落地性 |
| `prd-spec-enhancer` | 将 MRD/原始 PRD 补全为可开发的 PRD-SPEC | 当前在 AICoding 仓库内使用 | 工作流与模板已完成，尚未随安装器分发 | 提前暴露需求缺口，降低开发中途返工 |
| `code-reviewer` | 按规则库机械审查 Vue/TS 变更 | 仓库内按 diff、目录或文件调用 | 审查流程、规则库和报告模板已完成；未接入 CI | 统一 Review 口径，让问题可追溯、可复盘 |
| `engineering` | 识别复用、抽取和保留边界，规划资产沉淀 | 仓库内输出改造报告 | 诊断流程和参考资料已完成；默认不直接改代码 | 把重复经验转为可发现的组织资产，控制过早抽象 |
| `performance` | 采集症状、分类瓶颈并输出带指标的优化方案 | 仓库内输出性能报告 | 六类诊断路径和报告模板已完成；没有生产监控接入 | 将性能优化从经验猜测变为数据驱动的闭环 |
| `grilling` | 用决策树逐层拷问方案、设计和面试叙事 | 仓库内交互式使用 | 核心交互流程已定义；无独立脚本或自动化测试 | 暴露静默假设，提高方案评审和关键决策质量 |

> “当前使用情况”表示仓库已经支持的使用方式，不代表已经完成组织范围推广。项目目前没有 Skill 调用遥测，也没有真实业务试点数据。

### 6.2 `aicoding-codegen`：智能代码生成

**技能是什么**

把 AICoding 作为外部知识库，在当前业务仓库中完成真实前端代码生成。它先定位知识库、检索最相关 Prompt，再加载合同指定的 Rules 和 ai-kit 参考实现，最后检查业务项目事实并生成代码。

**执行流程**

```text
.aicoding/config.json 定位知识库
  → 自然语言检索 1–3 个相关 Prompt
  → 加载合同关联的 Rules / ai-kit
  → 检查业务仓库已有资产和依赖
  → 生成或修改代码
  → 执行目标项目的类型检查、测试、lint 或构建
  → 说明依据、改动和未验证项
```

**当前使用情况**

- 是 6 个 Skill 中唯一随当前安装器分发的 Skill；
- `npx aicoding install --target .` 会在业务项目生成 `.aicoding/config.json`、`.agents/skills/aicoding-codegen/` 和 `.cursor/rules/aicoding-codegen.mdc`；
- 安装器行为已有 Node 测试；
- 检索器已经使用 Prompt 资产合同作为权威来源；
- 尚无真实业务项目的规模化使用数据，也没有远程知识库版本管理。

**能给组织带来的成功**

- 把资深开发者的实现习惯固化为默认生成路径，减少团队成员之间的结果差异；
- 优先复用业务项目和 ai-kit 已有资产，降低重复实现和长期维护成本；
- 在生成前检查 API、类型、依赖和相似代码，降低“能生成但不能运行”的概率；
- 将生成依据显式报告出来，使 AI 代码更容易 Review 和追责；
- 为新人提供接近资深成员的默认工程路径，缩短熟悉团队规范的时间。

**建议指标**：任务交付周期、AI 生成后人工修改时长、首次验证通过率、ai-kit 复用率、Review 中重复实现问题数、未命中 Prompt 数。

### 6.3 `prd-spec-enhancer`：需求补全与审校

**技能是什么**

把 MRD、原始 PRD 或需求片段转化为可以直接指导研发的 PRD-SPEC。它通过检查清单判断必备、条件必备和可选元素，按输入、推理、AICoding 资产、人工确认的优先级补全内容。

**关键机制**

- 不编造真实字段、接口、权限或业务枚举；
- 推断内容标记 `≈推断`，真实缺口标记 `⚠️待确认`；
- 必须检查 ai-kit 复用机会和 Rules 约束；
- 将缺口集中提问，减少反复打断；
- 按统一模板输出完整 PRD-SPEC。

**当前使用情况**

- Skill、checklist、词典和 PRD-SPEC 模板均已存在；
- 当前主要在 AICoding 仓库内执行；
- 安装器尚未实现 `--with-spec`，不能一键复制到业务项目；
- HLD、LLD、TASKS 仍是模板驱动，尚未形成自动接力的已实现流水线。

**能给组织带来的成功**

- 把需求缺口从开发阶段前移到需求评审阶段；
- 统一产品、设计、研发和测试对验收条件的理解；
- 通过来源标注避免 AI 把推断包装成业务事实；
- 让需求文档天然包含复用和工程约束，减少后续方案返工；
- 为跨角色协作提供稳定、可评审的输入格式。

**建议指标**：开发开始后的需求变更次数、阻断级待确认项关闭周期、返工工时、验收遗漏数、PRD-SPEC 一次评审通过率。

### 6.4 `code-reviewer`：规则驱动代码审查

**技能是什么**

读取 git diff、文件或目录，根据 AICoding 规则库和 ai-kit 复用要求逐项审查，生成 Markdown 报告，并给出 PASS、FAIL 或 CONDITIONAL 结论。

**关键机制**

- 缺陷必须包含规则 ID、文件位置、问题和建议；
- Error、Warning、Info 分级聚合；
- 重复封装 ai-kit 已有能力按 Error 处理；
- 只审查、不自动修改代码，保持审批边界清晰。

**当前使用情况**

- Skill、机械规则库和报告结构已完成；
- 可针对当前 diff、目录、文件或特定功能调用；
- 当前是仓库内工作流，尚未随安装器分发；
- 尚未接入 PR/MR 门禁或 CI，因此现在是“可执行的人工触发审查”，不是自动质量门禁。

**能给组织带来的成功**

- 将 Review 标准从个人经验转化为统一、可追溯的规则；
- 把高频低价值检查交给 Agent，让 Reviewer 聚焦业务和架构风险；
- 形成缺陷类型数据，为规则优化和培训提供依据；
- 减少同类问题在多个项目和多个 Reviewer 之间反复出现。

**建议指标**：Review 平均耗时、每个 PR 的规则类问题数、同类缺陷复发率、进入人工 Review 前的拦截率、CONDITIONAL/FAIL 分布。

### 6.5 `engineering`：可复用资产沉淀

**技能是什么**

扫描业务代码中的重复模式，判断应该复用现有 ai-kit、抽取新资产，还是保留在业务代码中。对候选资产给出组件或 Hook 契约、AI 注释、Rules 同步项和改造优先级。

**关键机制**

- 先查 ai-kit，已有能力必须复用；
- 至少两个真实业务场景且语义稳定，才进入抽取候选；
- 单次出现只作为观察项，防止过早抽象；
- 新资产必须同步代码、AI 注释和 Rules，否则组织中的 AI 无法发现它；
- 默认只输出报告，不直接修改业务代码。

**当前使用情况**

- ai-kit 目录、抽取标准、注释模板和报告模板均已定义；
- 当前适合定期扫描业务模块或在大型需求结束后做复盘；
- 尚未形成自动采集跨项目重复度的数据平台，也没有资产晋级看板。

**能给组织带来的成功**

- 把个人代码经验持续沉淀为组织公共能力；
- 防止不同项目重复建设相同组件和 Hook；
- 用明确门槛控制公共资产质量，避免“为了复用而复用”；
- 让公共资产不仅能被人发现，也能被 AI 正确检索和使用；
- 建立“业务实践 → 识别模式 → 沉淀资产 → 更新规则 → 再生成”的正向循环。

**建议指标**：重复实现数量、公共资产覆盖场景数、资产复用项目数、候选资产转化率、因过早抽象产生的回退次数、未映射 ai-kit 资产数。

### 6.6 `performance`：性能诊断与改造

**技能是什么**

针对页面卡顿、首屏慢、内存增长、大数据、网络、计算和 ECharts 等问题，先采集症状，再分类瓶颈，最后输出带基线、目标、优先级和回滚预案的优化报告。

**关键机制**

- 没有症状和数据时不直接下结论；
- 按渲染、内存、大数据、网络、图表、计算六类路由参考资料；
- 优先使用 ai-kit 已有的 `useTable`、`useChart`、`BaseChart`、防抖节流和请求能力；
- 每条建议必须有具体位置、改造动作和验证指标；
- 默认不改代码，优化后应再次测量。

**当前使用情况**

- 六类诊断资料、症状采集流程和报告模板已经建立；
- 当前需要人工提供代码、复现步骤或 Performance/Memory/Lighthouse 等证据；
- 尚未接入生产监控、RUM 或性能数据平台，也没有组织基线。

**能给组织带来的成功**

- 将“感觉页面慢”转化为可复现、可量化、可回滚的工程问题；
- 复用统一优化方案，减少不同团队各自试错；
- 避免没有数据支撑的性能重构；
- 积累典型瓶颈和解决方案，形成组织性能知识库。

**建议指标**：LCP、TTI、INP、FPS、内存峰值、接口耗时、性能缺陷关闭周期、优化前后收益达标率。

### 6.7 `grilling`：决策树式压力测试

**技能是什么**

将方案、设计或面试叙事映射为决策树，按依赖关系逐轮追问当前可以决策的问题，直到没有静默假设。

**关键机制**

- 事实由 Agent 查找，不把可查事实推给用户；
- 决策由责任人确认，不由 AI 越权决定；
- 每轮只追问前置条件已经成立的问题；
- 可用于需求澄清、HLD/ADR 评审和面试压力测试；
- 达成共同理解后才进入执行。

**当前使用情况**

- 交互方法和与设计链、面试准备的结合方式已经定义；
- 当前是仓库内的对话式 Skill；
- 没有独立脚本、报告模板、自动化测试或业务项目安装能力。

**能给组织带来的成功**

- 在编码前暴露方案中的假设、依赖和未决项；
- 提高 ADR、架构评审和跨团队决策的透明度；
- 减少因为默认理解不同导致的返工；
- 帮助成员建立结构化表达和技术答辩能力。

**建议指标**：评审后新增重大决策数、开发中途方案反转次数、阻断项提前发现率、评审结论关闭周期。

## 7. Prompt、Rules 与 ai-kit 的最新闭环

最新完成的 Prompt Asset Contracts 解决了“检索结果为什么加载这些规则和代码”的可追溯问题。

`prompts/asset-contracts.json` 为全部 22 个正式 Prompt 明确声明：

- 必须加载的 Rules；
- 对应的 ai-kit References；
- 如果没有运行时参考资产，必须提供 `noReferenceReason`；
- 所有路径必须位于仓库内且真实存在；
- Prompt 正文引用与合同不一致时报告漂移；
- 未被任何 Prompt 映射的 Rules 和 ai-kit 会进入覆盖率告警，而不是静默丢失。

检索器优先使用合同作为权威来源；旧知识库缺少合同时仍可按目录和正文推断，但会返回 `CONTRACT_FALLBACK` 告警。这使项目同时获得确定性和向后兼容能力。

## 8. ai-kit 当前能力

| 类型 | 代表资产 | 已覆盖能力 |
| --- | --- | --- |
| 容器组件 | `BaseDialog`、`BaseDrawer` | loading 锁定、异步关闭、错误上报、响应式尺寸 |
| 表单与搜索 | `BaseForm`、`BaseSearch` | 校验、布局、搜索项折叠、移动端适配 |
| 数据展示 | `BaseChart`、`BaseTree` | loading、empty、error、retry、生命周期管理 |
| 级联筛选 | `CascadeFilter`、`useCascadeOptions` | 三级联动与懒加载选项链 |
| 状态 Hook | `useRequest`、`useTable`、`useTree` | 错误状态、重试、分页、最后请求优先 |
| 交互 Hook | `useDialog`、`useSearch`、`useChart` | Promise 收敛、防抖清理、图表创建与销毁 |
| 工具函数 | `src/ai-kit/utils/` | 防抖、节流、日期、格式、校验、存储、DOM、文件、树、数组、对象、字符串 |
| 页面模板 | `list-page-template.vue` | 可参与类型检查的列表页参考结构 |

ai-kit 的边界必须明确：它目前是生成参考和工程事实集合。业务项目已有公共资产时优先复用业务项目；只有目标项目已经复制、安装或配置对应 ai-kit 时，生成代码才能直接 import。

## 9. 已取得的项目成功

这些是当前已经有代码或测试证据支撑的成功：

1. **知识资产化**：规范、任务模板、参考实现和工作流进入 Git，可评审、可追踪、可回滚。
2. **关系确定化**：22 个 Prompt 全部拥有资产合同，Prompt、Rules 和 ai-kit 的加载关系可校验。
3. **跨项目入口建立**：Codegen Skill 可以安装到业务项目，知识库与业务代码保持边界。
4. **质量门控可执行**：类型检查、规则契约、Prompt 目录、Node 测试和组件/Hook 测试可以由统一命令执行。
5. **生成过程可解释**：Codegen 最终必须报告使用的 Prompt、Rules、参考资产、改动文件和验证结果。
6. **产品边界得到控制**：项目明确不是运行时业务系统、成熟组件库或已经验证收益的企业平台；打包检查也暴露了尚待修复的发布依赖缺口。

## 10. 如何给组织带来成功

组织成功不是“新增了多少 Prompt”，而是资产改变了交付行为，并最终改善业务结果。

| 组织目标 | AICoding 的作用机制 | 预期成功表现 | 需要采集的指标 |
| --- | --- | --- | --- |
| 提升研发效率 | Codegen 检索模板和参考实现，减少从零设计与重复编码 | 高频页面更快完成，人工二次修改减少 | 任务周期、AI 后修改时长、首次验证通过率 |
| 提升交付一致性 | Rules、Prompt 合同和 Code Review 使用同一事实来源 | 同类功能目录、状态和交互更一致 | 规则违规数、Review 返工轮次、同类实现差异数 |
| 降低质量风险 | 需求补全、生成约束、类型和测试将问题前移 | 缺陷在编码或 Review 前发现 | 阶段缺陷分布、缺陷逃逸率、回归缺陷数 |
| 提高资产复用 | ai-kit 与 Engineering Skill 形成复用和沉淀闭环 | 重复组件/Hook 减少，公共资产覆盖增加 | 复用率、重复实现数、资产覆盖项目数 |
| 缩短新人上手 | 默认工作流显式呈现组织规范和参考实现 | 新成员更少依赖口头指导 | 首个独立需求周期、规范类问题数、辅导时间 |
| 沉淀组织知识 | Prompt、Rules、Skill 和报告均可版本化 | 经验不再只存在于个人聊天和记忆 | 资产贡献人数、更新频率、命中率、失效资产数 |
| 改善协作决策 | PRD-SPEC 与 Grilling 暴露缺口和静默假设 | 产品、研发、测试更早达成一致 | 待确认关闭周期、开发中需求反转次数 |

### 10.1 推荐的组织落地方式

1. 选择 CRUD 列表、Dialog 表单、ECharts 图表三类高频任务作为首批试点；
2. 记录接入前两周基线，再记录使用 AICoding 后两到四周数据；
3. 同时保留人工 Review，避免把试点变成无监督生成；
4. 每周统计未命中 Prompt、错误引用、二次修改原因和规则误报；
5. 只有被至少两个真实业务场景验证的能力才进入 ai-kit；
6. 基于试点数据决定扩充 Prompt、修改 Rules 或新增 Skill，而不是先扩大功能范围。

### 10.2 成功判定原则

- 不只统计“生成次数”，还要统计能否通过验证和最终是否被采用；
- 不只看速度，还要看 Review 成本、缺陷和长期维护成本；
- 不把目标值写成已实现收益；
- 组织级结论至少需要多个任务、多个开发者和连续周期的数据；
- Skill 需要有负责人、版本、反馈入口和退出机制，才能成为稳定组织能力。

## 11. 当前验证结果

2026-08-20 实际执行 `npm run check`：

- Prompt 静态目录检查通过；
- 50 条 Cursor Rules、30 个 ai-kit 实现文件契约检查通过；
- `vue-tsc` 严格类型检查通过；
- Node 测试 22/22 通过，其中包含安装器、Prompt 合同、检索、Prompt 工具和工作台安全行为；
- Vitest 3 个测试文件、10/10 测试通过；
- Prompt 合同覆盖 22/22 正式 Prompt。

实际执行 `npm pack --dry-run --json`（使用临时 npm 缓存）成功生成 131 项发布清单，但清单未包含 `scripts/prompt-contracts.js`。由于 `scripts/search-prompts.js` 已直接依赖该文件，当前打包产物仍存在运行时依赖缺口，不能据此认定为发布就绪。

这些结果证明仓库自身的工程契约和核心工具链可运行，不等同于已经证明所有业务项目都能零修改接入。

## 12. 尚未完成与规划能力

### 12.1 尚未完成

- 项目仍为 `private: true`，尚未发布到内部或公共 npm；
- `package.json#files` 尚未包含 `scripts/prompt-contracts.js`，需要修复并增加打包产物运行验证；
- 仓库没有 CI，检查依赖开发者本地执行；
- 安装器没有 `--with-spec`，目前只分发 Codegen；
- `code-reviewer` 没有接入 PR/MR 自动门禁；
- Prompt Lab 基于 localStorage，不支持多人协作、权限和跨设备同步；
- 没有 Skill 调用遥测和组织级运营看板；
- 没有真实业务试点的效率、质量和复用收益数据。

### 12.2 已设计但尚未实现

- `hld-generator`、`frontend-lld-generator`、`dev-task-planner`；
- `spec-delivery-orchestrator` 和设计链机械校验器；
- 面向 AICoding 与业务代码的安全扫描、结构化切块和混合检索；
- CI、Spec 跨项目安装和更完整的 Prompt 质量门控。

设计文档和实施计划表示方向已经明确，但不能作为已交付能力统计。

## 13. 下一阶段路线

### P0：完成可信试点

- 先修复 npm 发布白名单缺失运行时依赖的问题，并增加从实际打包产物执行检索的测试；
- 选定三个高频、边界清晰的业务任务；
- 建立接入前基线与接入后数据；
- 记录生成、修改、Review、验证和缺陷全过程；
- 用试点数据验证哪些 Skill 真正改善交付。

### P1：补齐组织级交付基础

- 增加 CI，统一执行 `npm run check` 和打包检查；
- 发布内部 npm 或固定 Git tag，建立版本和变更日志；
- 实现 `--with-spec`，让需求补全能力可以安全接入业务项目；
- 为 Skills 建立 Owner、版本、反馈和度量机制；
- 将 code-reviewer 以非阻断模式接入 PR/MR，积累误报数据后再决定是否门禁。

### P2：按证据扩展

- 试点证明设计链价值后，再实现 HLD/LLD/TASKS 专业 Skills；
- 现有关键词检索出现稳定召回问题后，再引入安全代码扫描和混合检索；
- 至少两个真实业务场景验证后，再扩充上传、预览、虚拟表格等公共资产；
- 基于组织数据调整规则，而不是追求规则和 Prompt 数量。

## 14. 最终总结

AICoding 当前最有价值的成果，不是“AI 多写了多少代码”，而是建立了一套组织知识进入 AI 交付流程的工程方法：Rules 约束行为、Prompts 描述任务、Asset Contracts 固定关系、ai-kit 提供事实、Skills 编排流程、测试维护可信度。

项目已经具备仓库级闭环和 Codegen 跨项目入口。下一步的重点应从继续堆积资产转向真实业务试点、组织运营和数据验证。只有当开发周期、人工修改、Review 问题、复用率和缺陷数据持续改善时，AICoding 才从“完成的工程项目”升级为“给组织带来成功的研发基础设施”。
