# AI-AI-Coding 面试大纲

> 更新时间：2026-07-14  
> 使用方式：先记住 30 秒介绍和 3 分钟主线，再按面试官关注点进入架构、技术实现、实际使用、效率价值和量化评估。

## 1. 30 秒项目介绍

AI-AI-Coding 是我为 Vue 3 前端团队设计的一套 AI 辅助研发基础设施。它把可版本化 Prompt、50 条 Cursor Rules、15 个 ai-kit 实现文件、Codegen Skill、本地 Prompt Lab 和业务项目安装器连接起来。开发者在真实项目里说“生成一个折线图”，系统会先检索相关 Prompt 和规则，再检查业务项目已有依赖、API 和公共组件，最后在业务仓库生成并验证代码。它解决的不是“AI 能不能写代码”，而是“AI 生成的代码能否符合团队规范、复用已有资产并真正进入交付流程”。

## 2. 3 分钟项目讲解主线

建议按照“背景—方案—难点—结果—价值—边界”表达。

### 2.1 背景

团队直接使用 Codex 或 Cursor 时，AI 不知道内部资产，容易重复造轮子、虚构接口、遗漏异常处理。不同开发者的 Prompt 分散在聊天记录中，规范通常到 Code Review 才生效，返工较晚。

### 2.2 方案

我把能力拆成五层：

1. Prompts 描述具体任务；
2. Cursor Rules 约束架构、类型和场景规范；
3. ai-kit 提供可执行参考，而不是只有文档；
4. Codegen Skill 负责检索、检查业务项目、生成与验证；
5. Prompt Lab 负责 Prompt 的搜索、编辑、变量替换和本地沉淀。

### 2.3 难点

- 如何让 AI 只加载相关上下文，而不是塞入全部规则；
- 如何避免规则声明与真实组件能力漂移；
- 如何处理请求竞态、弹窗 Promise、图表生命周期等可运行性问题；
- 如何确保生成目标是业务仓库，而不是在知识库里做演示；
- 如何量化研发效率，而不是只说“感觉更快”。

### 2.4 当前结果

- 自然语言可以检索 Prompt，并返回相关 Rules 和 ai-kit references；
- 安装器可以把 Codex Skill、Cursor Rule 和配置写入业务项目；
- 50 条 Cursor 规则、15 个 ai-kit 文件通过契约和类型检查；
- Node 测试 9 项、Vitest 10 项通过；
- ECharts 升级至 6.1.0，最近一次安全审计 0 个漏洞；
- npm 打包预检确认 107 个必要文件进入发布清单。

### 2.5 价值和边界

当前已经证明工作流可执行，但还没有真实业务试点的长期数据。因此我会把“已完成的工程事实”和“待验证的业务收益”分开表达：仓库自身质量有测试证据，效率提升要通过 2—4 周试点采集开发周期、修改时长、Review 问题和缺陷数据。

## 3. 用 STAR 讲个人贡献

### S：Situation

团队虽然开始使用 AI 编码，但生成结果不稳定：同类页面重复实现，规范依赖人工提醒，Prompt 无法沉淀，AI 生成的代码与真实业务项目脱节。

### T：Task

我的目标是建立一条低成本、可渐进接入的 AI 研发工作流，让 AI 能发现团队资产、遵守规则、在业务项目生成代码，并且结果可以验证。

### A：Action

- 建立按场景拆分的 Prompt 与 Cursor Rules；
- 整理并修复 ai-kit 的基础组件和 Hook；
- 实现关键词检索和 Prompt—Rules—References 自动关联；
- 实现业务项目安装器与 `aicoding-codegen` Skill；
- 增加规则契约检查、Vue 类型检查和组件/Hook 自动测试；
- 用工程化 Skill 审核文档和代码是否一致；
- 定义效率、质量和业务响应指标，为真实试点做准备。

### R：Result

形成了可安装、可检索、可生成、可检查的闭环。当前本地完整检查通过，但业务价值仍需真实试点验证，因此不把目标提升比例包装成已取得结果。

## 4. 架构与设计追问

### 4.1 为什么不只写一个很长的系统 Prompt？

参考回答：

大 Prompt 会带来上下文噪声、规则冲突和维护困难。我将稳定约束放在 Rules，任务输入放在 Prompts，可执行事实放在 ai-kit，多步骤流程放在 Skills。检索器只返回与当前需求相关的 1—3 个 Prompt 和对应规则，既控制 token，也提高上下文相关性。

### 4.2 Rules、Prompts、ai-kit 和 Skills 的区别是什么？

- **Rules**：长期或场景约束，例如禁止 `any`、API 在 services 层适配；
- **Prompts**：一次任务需要的目标、字段、交互和验收信息；
- **ai-kit**：组件和 Hook 的真实参考实现，回答“具体怎样写”；
- **Skills**：包含触发条件、步骤、边界和交付格式的可执行工作流。

它们分别解决“约束、输入、事实和过程”，不能互相替代。

### 4.3 为什么 AICoding 不直接作为运行时组件库依赖？

参考回答：

不同业务项目的依赖版本、主题、目录和现有公共组件不同。如果强制业务代码直接依赖 AICoding，会增加耦合和升级风险。因此默认将它作为开发期知识库：AI 先参考 ai-kit，再优先适配业务项目已有资产。只有目标项目明确复制或配置了对应 ai-kit，才允许直接 import。

### 4.4 为什么还需要 Prompt Lab？

Prompt Lab 解决的是 Prompt 的可发现和可维护问题。它从 `prompts/**/*.md` 生成静态目录，支持搜索、分类、一键插入、变量替换、格式化、草稿和历史快照。它适合人工调试和沉淀 Prompt；自动生成链路则由 CLI、Skill 和 Cursor Rule 完成。

### 4.5 为什么检索先使用规则评分，而不是向量数据库？

当前 Prompt 规模只有 17 个，关键词、别名、标题、路径和正文评分已经可解释、低成本且便于测试。向量检索会增加服务、模型、索引和数据治理成本。只有在 Prompt 数量、同义表达和跨领域检索明显超出规则检索能力后，才值得引入混合检索。

## 5. 实际工作中怎么使用

### 5.1 接入步骤

```bash
cd /path/to/business-project
npm install --save-dev /path/to/AI-AI-Coding
npx aicoding install --target .
```

安装后业务项目会得到 `.aicoding/config.json`、Codex Skill 和 Cursor Rule。重新打开会话后，开发者可以直接提出需求。

### 5.2 一个真实任务如何执行

开发者输入：

```text
生成“设备在线趋势”折线图：
- 接口 getDeviceOnlineTrend({ startDate, endDate })
- 展示在线与离线两条线
- 支持 7 天和 30 天切换
- 包含 loading、空状态、错误重试和 resize
```

AI 的执行顺序是：

1. 读取业务仓库的 AICoding 配置；
2. 检索折线图 Prompt；
3. 读取全局、TypeScript、图表和折线图规则；
4. 检查业务项目已有图表组件、ECharts 版本、API 和类型；
5. 在业务项目创建或修改文件；
6. 运行该业务项目已有的 type-check、test、lint 或 build；
7. 说明命中的 Prompt、采用的规则、修改文件和未验证项。

### 5.3 如果业务项目已有 BaseChart 怎么办？

业务项目约定优先。AICoding 的 ai-kit 只是参考，不应为了使用 AICoding 而绕过业务项目已经稳定的封装。

### 5.4 如果接口或字段不明确怎么办？

先搜索业务仓库中的 services、types 和相似页面。仍然缺少关键业务信息时再询问用户，不能根据 Prompt 示例虚构 API、字段或权限。

## 6. Vue 与 TypeScript 技术追问

### 6.1 `useRequest` 如何处理请求竞态？

每次执行递增请求编号，只有最后一次请求可以更新 data、error 和 loading。这样搜索条件快速变化时，较慢的旧响应不会覆盖新结果。`cancel` 当前表示让响应失效；真正中止网络请求仍应由 service 层配合 `AbortSignal`。

### 6.2 为什么 `useTable` 要规定 `{ list, total }`？

公共 Hook 需要稳定契约。如果不同后端返回 `rows`、`records` 或嵌套 data，应该在 services 层适配，而不是让页面和 Hook 到处判断响应结构。这样分页和错误处理才可复用。

### 6.3 `useDialog` 为什么返回 Promise？

调用方可以像等待确认操作一样获得 true/false 结果，不必在多个组件之间手动传递回调。需要注意重复 open：当前实现会先用 false 结束上一次 Promise，避免悬空调用。

### 6.4 `BaseDialog` 如何防止提交状态丢失？

loading 时锁定遮罩、ESC、关闭图标和取消按钮；所有关闭入口统一经过 `beforeClose`；异步拦截异常通过 `close-error` 上报。这样不会在提交中误销毁表单。

### 6.5 `useChart` 为什么要同时处理挂载前状态和 dispose？

Vue watcher 可能在 DOM 挂载前传入 option 或 loading，因此 Hook 需要缓存并在 init 后补应用。组件卸载或主题切换时要清理 ECharts 实例、ResizeObserver、window 监听器和定时器，否则会产生内存和重复监听问题。

### 6.6 为什么不深度 watch 图表 option？

深度监听会遍历大型嵌套对象，高频数据下成本不可控。当前做法鼓励不可变更新，并通过 `updateDelay` 或 `setOption(option, delay)` 显式合并高频更新。

### 6.7 如何防止规则和代码漂移？

`scripts/validate-ai-kit.js` 会检查全部 `.mdc` 是否有合法 frontmatter、规则引用的 ai-kit 路径是否存在，以及 ai-kit 源码是否出现 `any`。再结合 `vue-tsc`、Node 测试和 Vitest，避免只靠人工文档审查。

## 7. 项目对研发效率产生什么价值

### 7.1 价值不是“少写几行代码”

更准确的价值链是：

```text
Prompt 与规则可复用
  → 减少重复沟通和起步时间
  → 优先复用已有资产
  → 减少人工修改和低价值 Review
  → 缩短到达可合并状态的时间
  → 需求更早上线，维护成本更低
```

### 7.2 可以回答的五类研发价值

1. **需求启动效率**：高频任务不必重新组织完整 Prompt；
2. **编码效率**：页面骨架、状态管理和边界处理有可复用参考；
3. **Review 效率**：规范检查前移，Reviewer 更关注业务逻辑；
4. **质量稳定性**：减少旧请求覆盖、资源未销毁和提交中误关闭等通用问题；
5. **组织效率**：把资深开发者经验沉淀成团队可复用资产，降低人员差异。

### 7.3 对业务产生什么价值？

参考回答：

AICoding 不直接改变收入指标，它通过研发效率和质量间接影响业务。常规需求更快到达可上线状态，可以缩短业务验证周期；统一组件和状态处理可以减少用户体验不一致；公共经验沉淀可以降低新人和跨团队协作成本；缺陷减少则降低回滚、线上支持和修复占用。

如果面试官追问收入价值，应说明只有当需求提前上线确实影响转化、留存或运营效率时，才能继续计算收入贡献，不能把节省研发时间直接等同于新增收入。

## 8. “多大的价值”应该怎么回答

### 8.1 当前项目能证明什么

可以确认的事实是：检索、安装、规则关联、ai-kit 契约、类型检查、测试和安全审计已经打通。当前不能确认的是“交付效率提高了 30%”这类业务结果，因为还没有真实团队试点数据。

推荐表达：

> 我没有把实验目标当成最终结果。当前先证明工具链可运行，下一步用相似任务做 2—4 周试点。只有开发周期下降且 Review 问题、返工和缺陷没有上升，才认定为有效收益。

### 8.2 价值估算公式

```text
单任务节省时间 = 接入前同类任务中位时长 - 接入后同类任务中位时长

月度毛节省工时 = 单任务节省时间 × 每月适用任务数

月度净节省工时 = 月度毛节省工时
                 - Prompt/规则维护工时
                 - 失败生成与返工增加工时

月度工程价值 = 月度净节省工时 × 团队综合小时成本
```

对于业务价值，可以进一步计算：

```text
交付提前天数 = 基线需求周期 - 试点需求周期

机会价值 = 提前天数 × 该功能经业务确认的日均增量价值
```

第二个公式必须有业务部门认可的增量价值，不能由研发自行假设。

### 8.3 一个可用于面试说明的假设案例

以下是估算示例，不是本项目已经取得的数据：

- 10 名开发者每月共完成 80 个适合使用 AICoding 的标准任务；
- 试点发现每个任务从 3.5 小时降至 2.5 小时；
- 月度毛节省为 `80 × 1 = 80` 小时；
- 扣除 20 小时规则维护和失败返工，净节省为 60 小时；
- 如果团队综合小时成本为 C，则年化工程价值约为 `60 × C × 12`。

同时必须验证 Review 问题密度和缺陷逃逸率没有上升，否则 60 小时只是把成本转移到后面，不是真实收益。

## 9. 价值应该怎么评估

### 9.1 评估设计

1. 选择 CRUD、图表、Dialog 等边界清晰的高频任务；
2. 以近 4—8 周同类任务建立基线；
3. 试点 2—4 周，记录 AICoding 是否命中、采用了哪些资产；
4. 比较相似复杂度任务，不把简单任务和复杂需求直接混合；
5. 同时观察效率指标和质量护栏；
6. 对失败任务分类，定位是 Prompt、规则、检索、资产还是业务上下文问题。

### 9.2 核心指标

| 类型 | 指标 | 计算方式或含义 |
| --- | --- | --- |
| 效率 | 到达可 Review 状态的时长 | 首次编码到提交可 Review PR |
| 效率 | 人工二次修改时长 | AI 初次生成后到本地检查通过 |
| AI | Prompt 命中率 | 正确命中任务数 / 使用 AICoding 的任务数 |
| AI | 首次生成可用率 | 无需推翻重写的任务数 / AI 生成任务数 |
| 复用 | 公共资产复用率 | 使用已有资产的适用任务数 / 适用任务数 |
| 质量 | Review 问题密度 | 规范和重复实现问题数 / PR 或千行代码 |
| 质量 | 返工率 | 因生成质量重新实现的任务数 / 生成任务数 |
| 稳定性 | 缺陷逃逸率 | 上线后归因于生成代码的缺陷数 / 生成任务数 |
| 组织 | 新人独立完成时间 | 从接手到独立完成标准任务的时间 |
| 成本 | 维护投入 | Prompt、规则、资产和检索器维护工时 |

### 9.3 如何判断试点成功

可以在试点前设目标，例如：开发中位时长下降 20%，首次生成可用率提高 15 个百分点，Review 规范问题下降 30%。这些是待验证目标，不是现有结果。

最终成功标准应同时满足：

- 交付时长显著下降；
- Review、返工和线上缺陷不升高；
- 维护成本低于节省工时；
- 开发者愿意持续使用，而不是为了试点被动执行。

### 9.4 如何排除“只是任务变简单了”？

回答要点：

- 按任务类型、字段数、接口数和交互复杂度分层；
- 使用同团队前后对比，条件允许时设置小范围对照组；
- 使用中位数和分位数，避免少数极端任务影响均值；
- 记录开发者熟练度和业务变更次数；
- 样本不足时只报告趋势，不下确定结论。

## 10. 面试官可能追问的价值问题

### 10.1 “这不就是 Prompt 仓库吗？”

参考回答：

如果只有 Markdown Prompt，价值确实有限。这个项目增加了自然语言检索、规则自动关联、ai-kit 可执行参考、业务项目安装器、Codex Skill、Cursor Rule 和验证链路。真正的差异是它能在业务仓库完成“检索—检查—生成—验证”，而不是让人手工复制一段 Prompt。

### 10.2 “AI 本身已经能写折线图，为什么需要你这个项目？”

参考回答：

通用 AI 能写一个折线图，但不知道公司项目用什么请求层、主题、权限、空状态、错误处理和公共组件。AICoding 的价值是减少这些项目上下文缺失造成的返工，并让生成结果遵循目标项目事实。

### 10.3 “维护规则的成本会不会超过收益？”

参考回答：

这是必须度量的风险。因此只沉淀高频、稳定场景；单一用例先观察，不立即抽象。评估净收益时必须扣除规则、Prompt 和资产维护工时。长期未命中、没有降低问题或维护成本过高的规则应该删除。

### 10.4 “如何证明缺陷减少是 AICoding 的贡献？”

参考回答：

记录每个试点任务使用的 Prompt、规则和公共资产，并对 Review 问题分类。重点观察 AICoding 直接约束的类别，例如重复封装、`any`、错误状态缺失、请求竞态和资源清理。不能把所有质量变化都归因于 AICoding。

### 10.5 “如果效率提高但缺陷增加呢？”

参考回答：

说明优化无效。节省的编码时间只是转移成 Review、返工或线上修复成本。效率指标必须与 Review 问题、返工率和缺陷逃逸率一起评估。

### 10.6 “这个项目能带来多少收入？”

参考回答：

当前不能直接给出收入数字。首先可以计算净节省工时和交付提前天数；只有业务确认某功能提前上线与转化、留存或运营成本之间存在可量化关系后，才能进一步计算收入或成本价值。

## 11. AI 工程化深入追问

### 11.1 如何控制上下文窗口？

采用渐进式披露：全局只保留稳定规则，检索器返回最相关 Prompt 和规则，Skill 再按 references 读取必要 ai-kit 文件，不一次性加载整个仓库。

### 11.2 如何评估 Prompt 和 Rule？

建立正常、边界和反例任务集，记录命中率、规则遵守率、资产复用率、首次生成可用率和人工修改量。固定模型与关键参数做回归，把失败分类为检索错误、规则冲突、上下文缺失、Prompt 表达问题或资产能力不足。

### 11.3 AI 生成代码最大的风险是什么？

- 虚构 API、字段、权限和依赖；
- 套用组件但不理解业务语义；
- 忽略异常、空状态、安全和清理；
- 代码通过静态检查但没有真实运行；
- 规则过多造成冲突或机械套用。

解决方式是让业务仓库事实优先，运行目标项目已有检查，并保留人工 Review 和业务验收。

### 11.4 为什么没有继续抽取 Upload、VirtualTable？

这些能力依赖鉴权、存储、数据规模和第三方组件，当前缺少至少两个稳定业务用例。提前抽象会制造万能组件。现阶段保留规则指导，等真实重复证据出现后再设计契约。

## 12. 项目不足怎么回答

建议如实回答：

1. 已完成本地安装与 npm 打包预检，但项目仍为 `private`，尚未发布内部版本；
2. 已有完整本地检查，但尚未配置 CI；
3. ai-kit 仍是参考实现集合，不是成熟版本化组件库；
4. 检索基于关键词和规则评分，Prompt 增多后可能需要混合检索；
5. Prompt Lab 仅使用 localStorage，不支持团队同步和权限治理；
6. 尚无真实业务试点的量化数据，价值模型已定义但结论待验证；
7. 生成结果仍需要目标项目测试、人工 Review 和业务验收。

改进顺序是：先做真实业务试点和指标采集，再接 CI 与内部版本发布，最后根据失败数据扩展检索和公共资产。

## 13. 面试前准备清单

- 能在 30 秒、3 分钟和 10 分钟三个时长下介绍项目；
- 能画出“业务需求 → Prompt → Rules/References → 业务项目生成 → 验证”流程；
- 现场演示一次 `aicoding search` 和业务项目安装结果；
- 能解释 `useRequest` 竞态、`useDialog` Promise、`useChart` 生命周期；
- 能说清“知识库依赖”和“运行时依赖”的区别；
- 记住当前验证事实：50 条规则、15 个实现文件、9 项 Node 测试、10 项 Vitest；
- 准备价值公式和一个明确标注为“估算示例”的案例；
- 不把试点目标说成已经取得的业务结果；
- 能解释维护成本、归因偏差和质量护栏。

## 14. 一句话收尾

这个项目体现的不是“我会让 AI 多写代码”，而是“我能把团队工程经验变成 AI 可检索、可执行、可验证的研发工作流，并用交付效率、复用率、Review 质量和缺陷数据评估它是否真的创造价值”。
