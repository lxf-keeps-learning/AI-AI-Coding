# AICoding 面试大纲

> 更新时间：2026-08-20
> 使用方式：先记住 30 秒介绍和 3 分钟主线，再按面试官关注点进入架构、技术实现、实际使用、价值与量化评估。
> 所有数量口径以当前命令输出为准（见第 15 节现场演示脚本）。

## 1. 30 秒项目介绍

AICoding 是我为 Vue 3 + TypeScript 团队设计的一套**开发期 AI 工程基础设施**。它不是让 AI 从 0 随便生成，而是让 AI 基于团队规范、可复用资产和真实业务项目生成代码。它把三样东西串起来：Cursor Rules（50 条生成约束）、可版本化 Prompt（22 个任务模板）、ai-kit（30 个组件和 Hook 的参考实现），再加一个检索器和一个安装器。开发者在真实项目里说“生成一个折线图”，系统先检索相关 Prompt 和规则，再检查业务项目已有依赖、API 和公共组件，最后在业务仓库生成并验证代码。它解决的不是“AI 能不能写代码”，而是“AI 生成的代码能否符合团队规范、复用已有资产并真正进入交付流程”。

## 2. 3 分钟完整主线

按“背景 → 方案 → 难点 → 结果 → 价值 → 边界”表达。

### 2.1 背景

团队直接用 Codex / Cursor 时，AI 不知道内部资产：重复造轮子、虚构接口、遗漏异常处理。Prompt 分散在聊天记录里无法沉淀，规范到 Code Review 才生效，返工太晚。

### 2.2 方案

把能力分成四层资产 + 两条链路：

- **四层资产**：Rules（不能怎么做）→ Prompts（一次任务要什么）→ ai-kit（具体怎么写）→ Skills（完整流程怎么走）；
- **两条链路**：需求链路（PRD-SPEC → HLD → LLD → TASKS）与编码链路（检索 → 规则/资产 → 生成 → 验证）；
- **安装器**：把 Codegen Skill、Cursor Rule、配置写入业务项目，AI 在业务仓库内完成生成与验证。

### 2.3 难点

- 如何让 AI 只加载相关上下文，而不是塞入全部规则；
- 如何避免规则声明与真实组件能力漂移；
- 如何处理请求竞态、弹窗 Promise、图表生命周期等可运行性问题；
- 如何确保生成目标是业务仓库，而不是在知识库里做演示；
- 如何量化研发效率，而不是只说“感觉更快”。

### 2.4 当前结果

- `npm run check` 全绿：语法、Prompt 目录、ai-kit 契约、vue-tsc、Node 9 项、Vitest 10 项全部通过；
- 安装器可把 Codegen Skill + Rule + 配置写入业务项目（默认只安装这三样）；
- `npm pack --dry-run` 通过，发布清单约 130 个文件，白名单排除测试/实验/过程文档。

### 2.5 价值和边界

当前证明了“仓库自身契约可执行”和“工具链可运行”，但**没有真实业务试点的长期数据**。因此我把“已完成的工程事实”和“待验证的业务收益”分开表达：效率提升要通过 2—4 周试点采集开发周期、修改时长、Review 问题和缺陷数据。

## 3. 项目背景与个人职责（STAR）

### S：Situation

团队开始使用 AI 编码，但结果不稳定：同类页面重复实现，规范依赖人工提醒，Prompt 无法沉淀，AI 生成与真实业务项目脱节。

### T：Task

建立一条低成本、可渐进接入的 AI 研发工作流：让 AI 能发现团队资产、遵守规则、在业务项目生成代码，并且结果可以验证。

### A：Action

- 建立按场景拆分的 Prompt 与 Cursor Rules；
- 整理并修复 ai-kit 基础组件和 Hook，补齐运行契约（竞态、Promise、生命周期）；
- 实现关键词检索与 Prompt—Rules—References 自动关联；
- 实现业务项目安装器与 `aicoding-codegen` Skill；
- 增加契约检查、vue-tsc、组件/Hook 测试；
- 把 `.cursor/rules/` 与 `src/ai-kit/` 提炼为 code-reviewer 机械审查规则；
- 定义效率、质量和业务响应指标，为试点做准备。

### R：Result

形成可安装、可检索、可生成、可检查的闭环，`npm run check` 全绿。业务价值仍需真实试点验证，不把目标提升比例包装成已取得结果。

## 4. 整体架构讲解

```
AICoding
├── 约束层   .cursor/rules/      ← 50 条 .mdc 生成约束
├── 输入层   prompts/            ← 22 个非索引 Prompt 模板
├── 事实层   src/ai-kit/         ← 30 个 .ts/.vue 参考实现
├── 流程层   skills/             ← 6 个 Skill（1 可安装，5 仓库内）
├── 需求层   design/             ← PRD-SPEC/HLD/LLD/TASKS 模板与样例
├── 工具层   scripts/            ← CLI / 检索器 / 安装器 / 目录生成 / 契约检查
├── 交互层   index.html          ← 本地 Prompt Lab
└── 验证层   tests/ + npm scripts
```

设计原则：

- **单一事实来源**：规范在 Rules，任务在 Prompts，事实在 ai-kit，流程在 Skills，不重复定义；
- **渐进式披露**：检索器只返回最相关的 1–3 个 Prompt，避免上下文爆炸；
- **发布边界唯一白名单**：`package.json#files` 精确控制 npm 包内容。

## 5. 为什么需要 Spec（PRD-SPEC）

通用 AI 拿到一句模糊需求直接写代码，结果不可控：字段凭空捏、边界缺失、无法验收。PRD-SPEC 把“模糊需求”变成“可指导研发直接开发的结构化文档”，在编码前就把缺口暴露出来。AICoding 里这由 `skills/prd-spec-enhancer/` 完成。

## 6. PRD-SPEC 如何工作

核心机制：**逐项检查 → 自动补全 → 自检 → 交互式收集缺口 → 渲染输出**。

- **必要性三级**：必备（恒定必须）/ 条件必备（命中触发条件才必须）/ 可选（复杂需求才需要）；
- **来源标注**：默认不标即已确认；`≈推断`（AI 补全需复核）、`⚠️待确认`（未定需补齐）；
- **不编造**：真实业务信息拿不到标 `⚠️待确认`，一次性集中问人；
- **复用优先**：R 板块恒定检查 ai-kit 复用清单。

边界：当前是仓库内 Skill，安装器尚未实现 `--with-spec`，不能一键安装到业务项目；HLD / LLD / TASKS 由 `design/_templates/` 模板驱动，不是三个独立自动化 Skill。

## 7. Prompt 如何组织、检索和验证

- **组织**：按场景分目录（charts / table / forms / tree / search / performance / review / refactor 等），每个 Prompt 描述任务目标、字段、交互、参考资产与验收。
- **检索**：`scripts/search-prompts.js` 综合自然语言关键词、别名、标题、路径和正文评分，返回命中 Prompt 及自动关联的 `rules` 与 ai-kit `references`。
- **验证**：`build-prompt-catalog.js` 生成静态目录，`check:prompts` 保证与源文件一致；检索测试覆盖折线图、CRUD、树、规则关联等代表性场景。
- **为什么不用向量数据库**：当前 Prompt 规模有限（22 个），关键词+别名+规则评分已可解释、低成本、易测试；向量检索会引入服务、模型、索引和数据治理成本。只有当数量、同义表达、跨领域检索明显超出规则检索能力后才值得升级。

## 8. Cursor Rules 与 ai-kit 如何协作

- **Rules 约束行为**：禁止 `any`、禁止裸用 `el-dialog`/`echarts.init`、API 放 services、页面 ≤500 行等；
- **ai-kit 提供事实**：`useRequest`/`useTable`/`BaseChart` 等 30 个真实实现，回答“具体怎么写”；
- **协作方式**：检索器把命中的 Prompt 和对应 Rules、references 一起返回；AI 先看 Rules 约束，再看 ai-kit 实现，最后在业务项目落地。
- **为什么 ai-kit 只是参考实现**：不同业务项目的依赖版本、主题、目录和现有公共组件不同。强制业务代码直接依赖 AICoding 会增加耦合和升级风险。默认 AI 参考 ai-kit 但优先适配业务项目已有资产，只有业务项目明确复制或配置对应 ai-kit 才允许 import。

## 9. 如何接入真实业务项目

```bash
cd /你的/业务项目
npm install --save-dev /AICoding所在路径/AI-AI-Coding
npx aicoding install --target .
```

安装器默认只生成三样：

```text
.aicoding/config.json
.agents/skills/aicoding-codegen/
.cursor/rules/aicoding-codegen.mdc
```

生成后 AI 执行：读配置 → 检索 Prompt → 读 Rules/ai-kit → 检查业务项目事实（依赖/相似代码/API/类型/目录）→ 在业务项目生成/修改代码 → 运行项目已有检查 → 汇报依据。

**如何避免 AI 虚构 API 和字段**：先搜索业务仓库的 services、types 和相似页面，仍然缺少关键业务信息时询问用户，绝不根据 Prompt 示例虚构。code-reviewer 也把“重复封装 ai-kit 已有能力”“裸用基础组件”记为 Error 来守住复用底线。

## 10. 最有技术含量的问题和解决方式

- **请求竞态**（`useRequest`）：每次执行递增请求编号，只有最后一次请求能更新 data/error/loading，慢的旧响应不会覆盖新结果；
- **弹窗 Promise**（`useDialog`）：调用方可 `await` 拿到 true/false；重复 open 会用 false 结束上一次 Promise，避免悬空；
- **提交状态丢失**（`BaseDialog`）：loading 时锁定遮罩/ESC/关闭图标/取消按钮，所有关闭入口统一经过 `beforeClose`；
- **图表生命周期**（`useChart`）：watcher 可能在 DOM 挂载前传入 option/loading，需缓存后补应用；卸载/主题切换时清理 ECharts 实例、ResizeObserver、监听器和定时器；
- **防规则漂移**：`validate-ai-kit.js` 检查 `.mdc` frontmatter、引用的 ai-kit 路径存在性、源码无 `any`，配合 vue-tsc 与测试。

## 11. 测试、契约与发布验证

```bash
npm run check
```

- `node --check`：全部 JS 语法；
- `check:prompts`：静态目录与源文件一致；
- `check:ai-kit-contracts`：50 条规则 frontmatter/引用路径合法、ai-kit 源码无 `any`；
- `type-check`：vue-tsc strict 全量检查 30 个实现文件；
- `test:node`：Node 9 项（检索、安装器、Prompt 工具）；
- `test:ai-kit`：Vitest 3 文件 10 项（utils / hooks / components）。

发布验证：`npm pack --dry-run`，`package.json#files` 是唯一白名单，确认不含 tests/实验/缓存/过程文档。

## 12. 项目价值及量化方法

### 机制价值（可以描述）

- 减少重复实现：loading/error/pagination/dialog/chart lifecycle 复用已有契约；
- 规范前移：类型、`any`、规则引用、目录漂移等由自动检查提前发现；
- 缩短反馈周期：AI 生成后运行目标项目已有检查，而不是只交付一段代码；
- 降低新人成本：Prompt、规则和代码实现共同展示团队认可的页面结构；
- 经验资产化：资深开发者经验沉淀为团队可复用资产，降低人员差异。

### 量化方法（已定义，未出结果）

价值模型已定义但**没有真实试点数据**，不能宣称效率提升 30% 之类的结果。建议 2—4 周试点采集：开发周期、人工二次修改时长、Prompt 命中率、首次生成可用率、公共资产复用率、Review 问题密度、返工率、缺陷逃逸率。比较相似复杂度任务，用同团队前后对比或小范围对照组，同时观察质量护栏（Review/返工/缺陷不升高）。

估算公式（示例，非已取得数据）：

```text
月度净节省工时 = 单任务节省时间 × 每月适用任务数 − 维护/返工工时
```

价值必须区分“已定义度量方法”和“已取得业务收益”：当前只有前者。

## 13. 当前限制

1. `private: true`，未发布到公共或内部 npm 仓库；
2. 有完整本地检查，但未配置 CI；
3. ai-kit 是参考实现集合，不是成熟版本化组件库；
4. 检索基于关键词和规则评分，非向量检索；
5. 安装器只装 Codegen，未实现 `--with-spec`；
6. HLD / LLD / TASKS 是模板驱动，非独立自动化 Skill；
7. Prompt Lab 用 localStorage，不支持跨设备同步与协作；
8. 无真实业务试点数据，效率/质量收益未验证；
9. 生成结果仍需目标项目测试、人工 Review 和业务验收。

## 14. 高频追问与参考回答

### 14.1 这和普通 Prompt 仓库有什么区别？

如果只有 Markdown Prompt，价值确实有限。这个项目增加了自然语言检索、规则自动关联、ai-kit 可执行参考、业务项目安装器、Codex Skill、Cursor Rule 和验证链路。真正的差异是它能在业务仓库完成“检索—检查—生成—验证”，而不是让人手工复制一段 Prompt。

### 14.2 为什么不用向量数据库？

当前 Prompt 只有 22 个，关键词+别名+标题+路径+正文评分已可解释、低成本、便于测试。向量检索增加服务、模型、索引和数据治理成本，只有数量和同义表达明显超出规则检索能力才值得引入混合检索。

### 14.3 为什么 ai-kit 只是参考实现？

不同业务项目依赖、主题、目录和公共组件不同。强制直接依赖 AICoding 增加耦合与升级风险。默认 AI 参考 ai-kit 但优先适配业务项目，只有业务项目明确复制或配置对应资产才允许 import。

### 14.4 如何避免 AI 虚构 API 和字段？

先搜索业务仓库的 services、types 和相似页面；仍然缺少关键信息时询问用户，不能根据 Prompt 示例虚构。prd-spec-enhancer 用 `≈推断`/`⚠️待确认` 标注，code-reviewer 机械检查复用合规。

### 14.5 Spec 为什么不会无限膨胀？

必要性三级判定：未命中触发条件的条件必备/可选元素不强求；PRD-SPEC 不是制式文件，避免过度膨胀。缺口走“一次性集中提问”，不反复打断。

### 14.6 Prompt 和 Cursor Rules 有什么区别？

Rules 是长期/场景约束（禁止 `any`、API 放 services），回答“不能怎么做”；Prompts 是一次任务的输入（目标、字段、交互、验收），回答“要什么”。Rules 稳定，Prompts 随任务变化，不能互相替代。

### 14.7 业务项目已有公共组件怎么办？

业务项目约定优先。ai-kit 只是参考，不应为了使用 AICoding 绕过业务项目已经稳定的封装。安装器也只生成配置文件，不覆盖业务项目文件。

### 14.8 如何证明生成结果质量？

不能只靠静态检查。仓库内验证：vue-tsc 0 错误 + 契约检查 + 测试；目标项目验证：生成后运行该业务项目已有 type-check/test/lint/build。最终质量仍由人工 Review 和业务验收确认。

### 14.9 为什么当前还不能证明效率提升？

没有真实业务试点数据。当前只有“工具链可运行”的工程事实，效率收益需要 2—4 周试点、同任务对比、中位数+分位数统计，且要排除“任务变简单”的偏差，同时观察质量护栏。

### 14.10 如何推广到其他项目？

开发依赖 + `npx aicoding install --target .`，AI 通过 `.aicoding/config.json` 解析知识库位置，不扫描用户磁盘。推广前提是团队能访问知识库路径，且发布为内部 npm 或固定 Git tag。

### 14.11 当前最大短板是什么？

最核心短板是**没有真实业务试点数据**，价值模型已定义但收益未验证。其次是尚未发布、无 CI、安装器只覆盖 Codegen、检索非向量化。

### 14.12 下一步为什么不是继续增加更多 Prompt？

规模不是目标。增加 Prompt 前要先回答三个问题：是否有高频场景覆盖缺口？检索器能否命中？维护成本是否可控？下一步应优先做真实业务试点用数据决定规则和 Prompt 去留，而不是堆数量。prompts 已经有 22 个覆盖主要场景，重点是验证命中率与复用率。

### 14.13 “AI 本身已经能写折线图，为什么需要这个项目？”

通用 AI 能写一个折线图，但不知道公司项目用什么请求层、主题、权限、空状态、错误处理和公共组件。AICoding 的价值是减少这些项目上下文缺失造成的返工，并让生成结果遵循目标项目事实。

### 14.14 如何控制上下文窗口？

渐进式披露：全局只保留稳定 Rules，检索器返回最相关的 1–3 个 Prompt 和对应规则，Skill 按 references 读取必要 ai-kit 文件，不一次性加载整个仓库。

### 14.15 维护规则的成本会不会超过收益？

这是必须度量的风险。只沉淀高频、稳定场景；单一用例先观察，不立即抽象。评估净收益时必须扣除规则、Prompt 和资产维护工时。长期未命中、没有降低问题或维护成本过高的规则应该删除。

### 14.16 这个项目能带来多少收入？

当前不能直接给收入数字。先算净节省工时和交付提前天数；只有业务确认某功能提前上线与转化、留存或运营成本之间有可量化关系后，才能继续计算收入价值。

## 15. 现场演示脚本

```bash
# 1. 全量验证（一条命令）
npm run check

# 2. 检索调试
npx aicoding search "折线图"
npx aicoding search "生成用户 CRUD 列表" --json

# 3. 安装到临时业务项目（验证安装器）
mkdir -p /tmp/demo-project
npx aicoding install --target /tmp/demo-project
ls -R /tmp/demo-project/.aicoding /tmp/demo-project/.agents /tmp/demo-project/.cursor

# 4. 发布边界
npm pack --dry-run
```

演示时可说：`npm run check` 全绿（Node 10 项 + Vitest 10 项 + 50 规则 + 30 实现文件通过），安装器生成三样文件，`npm pack --dry-run` 发布清单约 130 个文件且不含 tests/实验/过程文档。

## 16. 容易说错或不能夸大的内容

- ❌ “PRD-SPEC 可以一键安装到业务项目” → ✅ 目前是仓库内 Skill，安装器未实现 `--with-spec`；
- ❌ “HLD / LLD / TASKS 是三个独立自动化 Skill” → ✅ 是模板驱动；
- ❌ “Code Reviewer 输出 HTML 报告” → ✅ 输出 Markdown 报告；
- ❌ “效率提升 30%” → ✅ 无试点数据，只能描述机制价值；
- ❌ “ai-kit 是成熟组件库” → ✅ 是参考实现集合；
- ❌ “100% 防虚构” → ✅ 通过检索+标注+验证降低虚构，不承诺消除；
- ❌ 固定数量（“17 个 Prompt / 15 个文件 / 三个 Skill”）→ ✅ 以当前命令输出为准：22 个 Prompt / 30 个实现 / 6 个 Skill / 50 条规则。

## 17. 一句话收尾

这个项目体现的不是“我会让 AI 多写代码”，而是“我能把团队工程经验变成 AI 可检索、可执行、可验证的研发工作流，并且用交付效率、复用率、Review 质量和缺陷数据评估它是否真的创造价值”。
