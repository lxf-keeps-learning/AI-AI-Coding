# AICoding 面试大纲

> 更新时间：2026-08-20
> 使用方式：先掌握 30 秒介绍和 3 分钟主线，再按面试官关注点进入架构、Skill、工程验证、组织价值和技术取舍。
> 表达原则：严格区分“已实现”“仓库内可用”“可跨项目安装”“已设计未实现”“业务收益待验证”。

## 1. 面试表达总原则

这个项目最容易出现两类表达问题：一是只讲 Prompt 数量，显得像资料仓库；二是把规划和目标收益说成已经落地。正确主线应当是：

> 我解决的不是 AI 会不会写前端代码，而是 AI 如何基于组织知识，在真实业务项目中稳定、可解释、可验证地完成交付。

回答时遵守四点：

1. 先讲组织问题，再讲技术资产；
2. 先讲完整工作流，再讲单个组件；
3. 用当前测试和代码证明工程结果；
4. 主动说明没有业务试点数据，不虚构效率提升比例。

## 2. 30 秒项目介绍

AICoding 是我为 Vue 3 + TypeScript 团队设计的一套开发期 AI 工程基础设施。它把 50 条 Cursor Rules、22 个任务 Prompt、30 个 ai-kit 参考实现和 6 个 Agent Skill 连接起来，并通过 Prompt Asset Contracts 固定 Prompt、Rules 和代码资产之间的关系。开发者在业务项目里提出“生成一个折线图”后，Codegen Skill 会先检索组织知识，再检查项目现有依赖、API、类型和公共组件，最后在业务仓库生成并验证代码。它解决的核心问题不是代码生成速度，而是生成结果能否遵守团队规范、复用已有资产并进入真实交付流程。

## 3. 3 分钟完整主线

### 3.1 背景

团队开始使用 Codex、Cursor 后，通用模型不知道内部规范和公共资产。同类页面会重复实现，API 和字段可能被虚构，Prompt 分散在个人聊天里，很多问题只能到 Code Review 才发现。

### 3.2 方案

我把组织知识拆成五类：

- Rules：不能怎么做、必须遵守什么；
- Prompts：一次具体任务需要哪些输入、约束和验收；
- Asset Contracts：一个 Prompt 应加载哪些 Rules 和 ai-kit；
- ai-kit：团队推荐实现方式的真实代码；
- Skills：需求补全、代码生成、审查、性能诊断和资产沉淀的完整流程。

编码链路是：自然语言需求 → Prompt 检索 → 合同关联 Rules/ai-kit → 检查业务项目事实 → 在业务仓库生成 → 执行目标项目验证。

### 3.3 关键难点

- 如何只加载相关知识，避免上下文爆炸；
- 如何防止 Prompt、规则和真实代码之间漂移；
- 如何确保 AI 修改的是业务项目，而不是只在知识库里演示；
- 如何不虚构 API、权限、字段和依赖；
- 如何把请求竞态、Promise 收敛、资源销毁等运行时契约真正测起来；
- 如何评价组织价值，而不是只统计生成次数。

### 3.4 当前结果

- 50 条 Rules、22 个正式 Prompt、30 个 ai-kit 实现、6 个项目 Skill；
- 22/22 Prompt 已建立资产合同；
- Codegen Skill 可以安装到业务项目；
- `npm run check` 全绿：Node 22/22、Vitest 10/10、类型和契约检查通过；
- 项目仍未发布 npm、未配置 CI、未开展真实业务试点；当前打包白名单还缺少 `prompt-contracts.js` 运行时依赖。

### 3.5 组织价值

项目当前已经完成“组织知识资产化”和“工程闭环”。预期业务价值是缩短高频任务周期、减少人工修改和规则类 Review 问题、提高 ai-kit 复用率、缩短新人上手时间。因为还没有试点，我把这些作为待验证假设，并设计了基线和指标，而不是包装成已取得收益。

## 4. 项目背景与个人职责：STAR

### S：Situation

团队使用 AI 编码后出现结果不稳定、重复造轮子、规范生效晚、Prompt 无法沉淀、生成结果脱离真实业务项目等问题。

### T：Task

建立一条低成本、可渐进接入的 AI 研发工作流，让 Agent 能发现组织资产、遵守规则、在业务项目生成代码，并用工程手段验证结果。

### A：Action

- 建立按场景拆分的 Cursor Rules 和可版本化 Prompt；
- 整理 ai-kit 组件、Hook、工具和页面模板；
- 实现自然语言 Prompt 检索和跨项目安装器；
- 编写 `aicoding-codegen`，让 Agent 在目标业务仓库生成并验证；
- 建立 Prompt Asset Contracts，消除 Prompt、Rules 和 References 的隐式映射；
- 增加类型检查、契约检查、Node 测试和组件/Hook 测试；
- 建立 PRD 补全、代码审查、资产沉淀、性能诊断和方案压力测试 Skills；
- 定义组织试点的效率、质量、复用和协作指标。

### R：Result

形成了可安装、可检索、可生成、可检查的仓库级闭环，并建立了组织知识进入 Agent 工作流的工程方法。当前工程验证全绿，但真实业务效率和质量收益仍需试点验证。

## 5. 架构讲解

```text
AICoding
├── .cursor/rules/              50 条生成与审查约束
├── prompts/                    22 个任务 Prompt
├── prompts/asset-contracts.json
│                                Prompt → Rules → ai-kit 权威映射
├── src/ai-kit/                 30 个参考实现文件
├── skills/                     6 个工作流
├── design/                     设计模板与样例
├── scripts/                    检索、安装、目录和契约检查
├── index.html + workbench/     本地交互工具
└── tests/                      行为、类型和契约验证
```

### 5.1 为什么分层

- Rule 与 Prompt 分开：规则是长期约束，Prompt 是一次任务的输入和验收；
- Prompt 与 ai-kit 分开：Prompt 描述“做什么”，ai-kit 提供“组织已经怎么做”；
- Skill 与资产分开：Skill 编排流程，不复制全部知识；
- 知识库与业务仓库分开：AICoding 提供参考，最终代码必须适配目标项目事实。

### 5.2 单一事实来源

项目不是简单地把文件放进几个目录，而是要求每类信息有明确归属：规则在 Rules，任务在 Prompts，映射在 Asset Contracts，实现事实在 ai-kit，流程在 Skills。这样每类资产可以独立维护，又能通过合同和测试连接起来。

## 6. 六个 Skill 如何讲

### 6.1 一张表概括

| Skill | 面试时的一句话 | 当前状态 | 组织成功方向 |
| --- | --- | --- | --- |
| `aicoding-codegen` | 基于组织知识在业务项目生成并验证前端代码 | 唯一可安装 Skill | 规范一致、资产复用、减少二次修改 |
| `prd-spec-enhancer` | 把模糊需求补全为可开发、可验收的 PRD-SPEC | 仓库内可用 | 问题前移、减少需求返工 |
| `code-reviewer` | 用明确规则对代码变更做机械审查并给出结论 | 仓库内可用，未接 CI | 统一 Review 口径、减少重复问题 |
| `engineering` | 判断重复逻辑应该复用、抽取还是保留 | 仓库内报告型 Skill | 持续形成高质量公共资产 |
| `performance` | 先采集数据、再分类瓶颈、最后给可度量方案 | 仓库内报告型 Skill | 让性能治理可测量、可回滚 |
| `grilling` | 用决策树逐层暴露方案中的静默假设 | 仓库内对话型 Skill | 提高需求和架构决策质量 |

### 6.2 `aicoding-codegen`

**标准回答**

它不是一个大而全的代码生成 Prompt，而是一个渐进式上下文工作流。先从 `.aicoding/config.json` 找到知识库，用完整需求检索最相关的 1–3 个 Prompt，加载合同声明的 Rules 和 ai-kit，然后检查业务项目自己的依赖、API、类型、目录和相似实现。业务项目约定优先，ai-kit 默认只作为参考，最后执行目标项目已有验证并报告依据。

**设计亮点**

- 不扫描整块磁盘猜知识库位置；
- 不一次加载全部资产；
- 不允许从业务代码直接引用知识库绝对路径；
- 不虚构接口和依赖；
- 安装器和检索行为有测试。

**边界**

当前只有 Codegen 随安装器分发，没有内部 npm 版本管理，也没有真实业务规模数据。

### 6.3 `prd-spec-enhancer`

**标准回答**

它将 MRD 或原始 PRD 按 checklist 补全为 PRD-SPEC。必备项必须完整，条件必备项只在命中触发条件时要求，推断内容标 `≈推断`，无法获取的真实业务信息标 `⚠️待确认`。所有缺口集中确认，避免边生成边频繁打断。

**为什么有价值**

它把问题发现从编码阶段前移到需求阶段，并把 ai-kit 复用、异常状态和验收标准写进研发输入。

**边界**

当前没有 `--with-spec`；HLD、LLD、TASKS 是模板，不是已经实现的自动化 Skills。

### 6.4 `code-reviewer`

**标准回答**

输入是 git diff、文件或目录，按规则库扫描 Vue/TS 代码，输出包含规则来源、文件位置、严重级别和修复建议的 Markdown 报告。Error 为 0 时 PASS，有 Error 时 FAIL，只有 Warning 时 CONDITIONAL。它默认不改代码，避免审查者和修改者职责混在一起。

**组织价值**

把 Reviewer 的重复经验转成可追溯规则，让人工 Review 聚焦业务和架构。

**边界**

当前未接 CI 或 PR 门禁，也没有误报率数据。

### 6.5 `engineering`

**标准回答**

它用于公共资产沉淀，不是泛化的工程配置 Skill。先检查 ai-kit 是否已有能力；能覆盖就复用。只有至少两个真实业务场景重复且语义稳定，才建议抽取；只出现一次的模式保留为观察项。候选资产必须同时设计 TypeScript 契约、AI 注释和 Rules，否则 Agent 无法发现新资产。

**核心取舍**

重复代码有成本，但过早抽象的成本可能更高，所以公共资产进入 ai-kit 需要场景证据。

### 6.6 `performance`

**标准回答**

它拒绝在没有数据时直接给通用优化建议。先采集复现、数据量和 Performance/Memory 证据，再路由到渲染、内存、大数据、网络、图表或计算，最后输出带文件位置、改造动作、前后指标和回滚预案的报告。

**组织价值**

把性能问题从主观判断转成可复现、可度量的闭环，并优先复用团队已有优化能力。

**边界**

当前没有生产监控、RUM 和统一性能基线。

### 6.7 `grilling`

**标准回答**

它把方案或叙事映射为决策树，只追问前置条件已经确定的问题。事实由 Agent 在环境中查找，真正的产品或架构决策由责任人确认。它适合需求澄清、ADR 压力测试和面试模拟。

**组织价值**

让静默假设在实施前暴露，降低中途方案反转和跨角色理解偏差。

**边界**

它目前是对话式工作流，没有独立脚本和测试。

## 7. Prompt Asset Contracts：最新技术亮点

### 7.1 为什么需要合同

早期检索可以根据目录名和 Prompt 正文推断相关规则，但这种隐式关联不够稳定：目录调整、正文遗漏或新增资产都可能导致加载错误，而且很难解释为什么返回这些参考文件。

### 7.2 解决方案

为 22 个正式 Prompt 建立版本化 JSON 合同，每条合同明确：

- Rules 列表必须非空、唯一、路径合法且真实存在；
- References 必须唯一且真实存在；
- 没有运行时资产时必须说明原因；
- Prompt 正文引用未进入合同时判定漂移；
- 未映射资产进入覆盖率告警；
- 旧仓库没有合同时保留推断逻辑并返回回退告警。

### 7.3 面试官可能追问：为什么不用自动推断

自动推断适合冷启动和兼容旧资产，不能作为长期事实来源。权威合同提供确定性、可评审性和可测试性；推断只作为降级策略。这相当于把“隐式约定”升级为“显式接口”。

## 8. ai-kit 技术深挖

### 8.1 `useRequest` 请求竞态

问题：较早请求较晚返回，会覆盖新请求结果。

处理：为请求维护递增标识，只允许最后一次请求更新共享状态，同时统一管理 loading、data 和 error。

回答重点：不是简单封装 `try/catch`，而是定义并验证并发语义。

### 8.2 `useDialog` Promise 收敛

问题：弹窗关闭、取消、确认和异常路径可能让 Promise 永久 pending 或重复 resolve。

处理：统一管理会话状态，确保一次打开只产生一次确定结果，并处理重复打开和关闭边界。

### 8.3 `useChart` / `BaseChart` 生命周期

问题：隐藏容器初始化、重复 resize、未 dispose 会造成尺寸错误和内存泄漏。

处理：管理挂载前配置缓存、实例创建、resize 和销毁，补齐 loading、empty、error、retry 状态。

### 8.4 ai-kit 为什么不是组件库

当前没有独立主题、完整文档站、版本兼容承诺和运行时发布策略。它主要是组织推荐实现的事实源和代码生成参考。把它说成成熟组件库会夸大边界。

## 9. 检索设计与技术取舍

### 9.1 当前方案

检索器综合关键词、别名、标题、路径和正文评分，返回最相关 Prompt；Rules 与 References 由资产合同提供。

### 9.2 为什么暂时不用向量数据库

- 当前只有 22 个正式 Prompt，关键词和别名成本低且结果可解释；
- 可以稳定编写排序和关联测试；
- 不引入模型、索引、网络、权限和数据同步成本；
- 当前主要问题是资产准确性和业务验证，而不是检索规模。

### 9.3 什么时候升级混合检索

当 Prompt 和业务代码规模增长，并且实际数据证明同义表达召回不足、需要定位业务项目相似实现时，再引入安全扫描、结构化切块和混合排名。仓库已有设计与实施计划，但当前不能作为已实现能力介绍。

## 10. 工程验证怎么讲

统一入口是：

```bash
npm run check
```

当前实际结果：

- Prompt 静态目录一致；
- 50 条 Rules、30 个 ai-kit 实现文件契约检查通过；
- `vue-tsc` 类型检查通过；
- Node 测试 22/22；
- Vitest 3 个测试文件、10/10；
- Prompt 合同覆盖 22/22。

测试覆盖的代表性问题：

- 业务项目安装结果是否正确；
- 中文需求能否命中折线图、CRUD、树等 Prompt；
- 资产合同缺失、孤儿条目、重复规则、路径越界和引用漂移是否会被发现；
- 没有合同时是否安全降级并发出告警；
- Prompt Lab 变量、格式化、历史数据和密钥默认行为；
- ai-kit 组件、Hook 和工具的关键行为。

主动说明：仓库没有 CI，所以这是本地质量门控，不是已经部署的自动流水线。

打包检查可以生成 131 项清单，但当前 `package.json#files` 没有包含 `scripts/prompt-contracts.js`，而检索器已经依赖该文件。因此 `npm pack --dry-run` 成功只证明 npm 能组成包，不能证明包内运行时依赖完整。这个问题应在发布前修复，并用真实打包产物执行检索测试。

## 11. 如何回答“给组织带来了什么成功”

### 11.1 推荐回答

我把组织成功分成两个层次。第一层是已经完成的基础能力：团队规范、Prompt、参考实现和工作流已经版本化；Prompt 与资产关系可校验；Codegen 可以跨项目安装；统一检查全绿。这说明组织知识已经能稳定进入 Agent 工作流。

第二层是需要业务试点验证的结果：开发是否更快、人工修改是否减少、Review 问题是否下降、资产复用是否提高、新人是否更快上手。项目当前还没有足够真实数据，所以我没有给出虚假的提升百分比，而是设计了试点、基线和指标。

### 11.2 价值链

| 工程能力 | 行为变化 | 组织结果 | 指标 |
| --- | --- | --- | --- |
| Codegen + Prompt 检索 | 从零生成变为基于资产生成 | 高频交付更快、更一致 | 周期、修改时长、首次验证通过率 |
| Rules + Code Reviewer | 规范从人工提醒变为生成和审查约束 | 规则类问题减少 | Review 轮次、规则缺陷数、复发率 |
| PRD-SPEC | 缺口在编码前集中暴露 | 需求返工减少 | 需求变更、待确认周期、返工工时 |
| ai-kit + Engineering | 重复模式进入公共资产 | 复用提高、维护面缩小 | 复用率、重复实现、覆盖项目数 |
| Performance | 优化有基线、目标和回滚 | 性能治理可验证 | LCP、INP、FPS、内存、达标率 |
| Grilling | 决策假设提前显性化 | 中途方案反转减少 | 决策关闭周期、方案反转次数 |

### 11.3 试点方案

- 场景：CRUD 列表、Dialog 表单、ECharts 图表；
- 周期：先采集两周基线，再进行两到四周试点；
- 对象：至少覆盖不同经验水平的开发者；
- 数据：开发周期、AI 后人工修改、首次检查通过率、Review 问题、缺陷、Prompt 命中和 ai-kit 复用；
- 结论：速度和质量同时改善才算成功，只提升生成速度但增加 Review 或维护成本不算成功。

## 12. 高频追问与回答要点

### Q1：Rules、Prompt、ai-kit 和 Skill 有什么区别？

Rules 是长期约束，Prompt 是任务合同，ai-kit 是实现事实，Skill 是执行流程，Asset Contract 负责把一次任务需要的约束和事实明确连接起来。

### Q2：为什么不写一个超级 Prompt？

超级 Prompt 难维护、难测试、上下文成本高。分层后每类资产有单一职责，检索时只加载相关内容，变更影响更可控。

### Q3：如何防止 AI 幻觉？

先检查业务仓库事实，不虚构 API、字段、权限和依赖；知识引用由资产合同约束；真实业务信息缺失时明确暂停或标记待确认；生成后执行目标项目验证。

### Q4：为什么只有 Codegen 可以安装？

先收敛一条最核心的跨项目链路，控制发布和覆盖风险。其他 Skills 目前在知识库内可用，但安装策略、路径契约、版本升级和冲突处理还没有完成，所以不把“文件存在”当成“可安全分发”。

### Q5：你如何避免公共组件过度抽象？

先判断 ai-kit 是否已有能力；新抽取至少需要两个真实场景且语义稳定；单次出现只做观察项；公共资产必须同时具备类型契约、AI 注释和 Rules。

### Q6：为什么当前没有声称效率提升？

仓库测试只能证明工程契约可执行，不能证明组织收益。效率结论需要跨任务、跨人员和连续周期的业务数据，否则百分比只是目标，不是结果。

### Q7：这个项目最大的技术难点是什么？

不是写页面模板，而是维护组织知识之间的一致性和边界：什么是约束、什么是事实、如何只加载相关上下文、如何在业务项目落地、如何验证引用没有漂移。Prompt Asset Contracts 是对此的关键工程化处理。

### Q8：最大的不足是什么？

没有 CI、内部发布、真实业务试点和组织运营机制；当前发布白名单还缺少检索器的运行时依赖；5 个 Skills 仍是仓库内能力；ai-kit 不是成熟组件库；关键词检索在规模扩大后可能遇到召回瓶颈。

### Q9：如果让你继续做，优先做什么？

优先真实业务试点和 CI，不会先堆更多 Prompt。先证明 Codegen 在三个高频场景里对周期、修改和 Review 有正收益，再决定是否扩展 Spec 安装、Code Reviewer 门禁和混合检索。

### Q10：如何治理 Skill 本身？

每个 Skill 需要明确触发条件、输入输出、禁止行为、Owner、版本、测试或评估集、反馈入口和退出机制。没有度量和维护责任的 Skill 很快会成为新的过期文档。

## 13. 当前能力、规划能力和禁止夸大的说法

### 已实现

- 50 条 Cursor Rules；
- 22 个正式 Prompt 和 22/22 资产合同；
- 30 个 ai-kit 实现文件；
- 6 个仓库内 Skill；
- Codegen 跨项目安装；
- Prompt 检索、Prompt Lab 和本地验证体系。

### 已设计但未实现

- HLD、前端 LLD、TASKS 三个专业 Skill；
- 设计链编排器和机械校验器；
- 业务代码安全扫描、结构化切块和混合检索；
- `--with-spec`、CI 和自动 PR 门禁。

### 面试中不要说

- “6 个 Skill 都已经在业务项目规模化使用”；
- “HLD/LLD/TASKS 已经能全自动生成”；
- “已经提升研发效率 30%/50%”；
- “ai-kit 是成熟的内部组件库”；
- “已经发布为企业级平台”；
- “所有业务项目都能零修改接入”。

## 14. 现场演示脚本

### 14.1 仓库健康检查

```bash
npm run check
```

讲解顺序：Prompt 目录 → Rules/ai-kit 契约 → 类型检查 → Node 测试 → Vitest。

### 14.2 Prompt 资产合同

```bash
node scripts/prompt-contracts.js --check
```

讲解：22/22 覆盖、映射规则、路径存在性、引用漂移和未映射资产告警。

### 14.3 Prompt 检索

```bash
node scripts/search-prompts.js "生成一个支持时间范围和重试的折线图" --json --limit 3
```

讲解：命中 Prompt、全局 Rules、场景 Rules、`BaseChart`、`useChart`、`useRequest`。

### 14.4 业务项目安装

```bash
node scripts/aicoding.js install --target /path/to/business-project
```

讲解只生成三类受管理文件：配置、Codegen Skill、Cursor Rule。不要演示不存在的 `--with-spec`。

### 14.5 打包边界

```bash
npm pack --dry-run
```

讲解 `package.json#files` 白名单如何避免测试、实验和过程文档进入发布包。

同时主动指出：当前清单缺少 `scripts/prompt-contracts.js`，说明 dry-run 不能替代“从真实包安装并执行”的发布验收。

## 15. 最后的收口表达

如果面试官问“你从这个项目学到了什么”，可以这样回答：

> 我最大的认识是，AI Coding 的难点不是再写一个更长的 Prompt，而是把组织知识变成有边界、有版本、有引用关系、有验证和有反馈的数据与流程。只有这样，AI 才能从个人效率工具变成组织交付能力。AICoding 已经完成了工程闭环，但我也主动保留了业务价值验证这一步，因为真正的成功必须体现在交付周期、质量、复用和协作数据上。
